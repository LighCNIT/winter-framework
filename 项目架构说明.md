# Winter Framework 项目完整分析

## 一、项目概述

**Winter Framework** 是一个仿照 Spring Framework 设计的轻量级 IoC（控制反转）容器框架。这是一个学习型项目，旨在理解 Spring 的核心设计理念和实现原理。

### 技术栈
- **语言**: Java 8
- **构建工具**: Maven
- **测试框架**: JUnit 4 + AssertJ

---

## 二、核心概念

### 2.1 IoC (控制反转)
传统开发中，对象的创建和依赖关系由程序主动控制。IoC 将这个控制权交给容器，由容器负责对象的创建和依赖注入。

### 2.2 Bean
在 IoC 容器中管理的对象称为 Bean。容器负责 Bean 的整个生命周期。

### 2.3 BeanDefinition
Bean 的元数据信息，描述了如何创建一个 Bean（类型、属性、依赖、作用域等）。

### 2.4 Bean作用域 (Scope)
Bean的作用域定义了Bean实例的生命周期和创建方式：
- **Singleton（单例）**: 容器中只有一个实例，每次获取都返回同一个对象
- **Prototype（原型）**: 每次获取都创建新的实例，容器不管理原型Bean的生命周期

### 2.5 FactoryBean
FactoryBean是一种特殊的Bean，用于创建复杂对象或需要特殊初始化逻辑的对象：
- **延迟创建**: 只有在实际获取Bean时才调用getObject()方法
- **作用域支持**: 通过isSingleton()方法控制返回对象的作用域
- **对象分离**: FactoryBean本身和其创建的对象是分离的
- **缓存机制**: 单例FactoryBean的对象会被缓存，避免重复创建

---

## 三、项目架构

### 3.1 分层结构

```
org.winterframework.beans
│
├── BeanException.java                              # 异常类
│
├── factory/
│   ├── BeanFactory.java                           # 核心接口：Bean工厂
│   ├── FactoryBean.java                           # FactoryBean接口 ⭐ 新增
│   ├── Aware.java                                 # Aware标记接口 ⭐ 已实现
│   ├── BeanFactoryAware.java                      # BeanFactory感知接口 ⭐ 已实现
│   ├── InitializingBean.java                      # Bean初始化回调接口 ⭐ 已实现
│   ├── DisposableBean.java                        # Bean销毁回调接口 ⭐ 已实现
│   │
│   ├── config/
│   │   ├── BeanDefinition.java                    # Bean定义（元数据）
│   │   ├── BeanFactoryPostProcess.java            # BeanFactory后置处理器接口 ⭐ 新增
│   │   ├── BeanPostProcessor.java                 # Bean后置处理器接口 ⭐ 新增
│   │   ├── ConfigurableBeanFactory.java           # 可配置Bean工厂接口 ⭐ 新增
│   │   ├── AutowireCapableBeanFactory.java        # 自动装配Bean工厂接口 ⭐ 新增
│   │   └── SingletonBeanRegistry.java             # 单例注册表接口
│   │
│   ├── support/
│   │   ├── DefaultSingletonBeanRegistry.java      # 单例注册表实现
│   │   ├── AbstractBeanFactory.java               # 抽象Bean工厂
│   │   ├── AbstractAutowireCapableBeanFactory.java # 可自动装配的Bean工厂
│   │   ├── DefaultListableBeanFactory.java        # 默认可列表的Bean工厂
│   │   ├── BeanDefinitionRegistry.java            # Bean定义注册表接口
│   │   ├── BeanDefinitionReader.java              # Bean定义读取器接口 ⭐ 新增
│   │   ├── AbstractBeanDefinitionReader.java      # Bean定义读取器抽象基类 ⭐ 新增
│   │   ├── InstantiationStrategy.java             # Bean实例化策略接口
│   │   ├── SimpleInstantiationStrategy.java       # 简单实例化策略（JDK反射）
│   │   ├── CglibSubclassInstantiationStrategy.java # Cglib实例化策略（动态代理）
│   │   └── DisposableBeanAdapter.java             # Bean销毁适配器 ⭐ 新增
│   │
│   └── xml/
│       └── XmlBeanDefinitionReader.java           # XML Bean定义读取器 ⭐ 新增
│
org.winterframework.context
│
├── ApplicationContext.java                        # 应用上下文接口
├── ApplicationContextAware.java                   # ApplicationContext感知接口 ⭐ 新增
├── ApplicationEvent.java                          # 应用事件抽象基类 ⭐ 新增
├── ApplicationListener.java                       # 应用事件监听器接口 ⭐ 新增
├── ApplicationEventPublisher.java                 # 应用事件发布者接口 ⭐ 新增
├── ConfigurableApplicationContext.java            # 可配置应用上下文接口
│
├── event/
│   ├── ApplicationEventMulticaster.java          # 应用事件广播器接口 ⭐ 新增
│   ├── AbstractApplicationEventMulticaster.java  # 应用事件广播器抽象基类 ⭐ 新增
│   ├── SimpleApplicationEventMulticaster.java    # 简单应用事件广播器实现 ⭐ 新增
│   ├── ApplicationContextEvent.java              # 应用上下文事件抽象基类 ⭐ 新增
│   ├── ContextRefreshedEvent.java                # 容器刷新完成事件 ⭐ 新增
│   └── ContextClosedEvent.java                   # 容器关闭事件 ⭐ 新增
│
└── support/
│   ├── AbstractApplicationContext.java            # 应用上下文抽象基类
│   ├── AbstractRefreshableApplicationContext.java # 可刷新应用上下文抽象类
│   ├── AbstractXmlApplicationContext.java         # XML应用上下文抽象类
│   ├── ApplicationContextAwareProcessor.java      # ApplicationContextAware处理器 ⭐ 新增
│   └── ClassPathXmlApplicationContext.java        # ClassPath XML应用上下文实现

org.winterframework.core.io
│
├── Resource.java                                   # 资源访问接口
├── ResourceLoader.java                             # 资源加载器接口
├── DefaultResourceLoader.java                     # 默认资源加载器实现
├── ClassPathResource.java                         # ClassPath资源实现
├── FileSystemResource.java                        # 文件系统资源实现
└── UrlResource.java                               # URL资源实现

org.winterframework.aop
│
├── AdvisedSupport.java                            # AOP通知支持类 ⭐ 新增
├── TargetSource.java                              # 目标对象源 ⭐ 新增
├── MethodMatcher.java                             # 方法匹配器接口 ⭐ 新增
├── ClassFilter.java                               # 类过滤器接口 ⭐ 新增
├── Pointcut.java                                  # 切点接口 ⭐ 新增
│
├── aspectj/
│   └── AspectJExpressionPointcut.java             # AspectJ表达式切点实现 ⭐ 新增
│
└── framework/
    ├── AopProxy.java                              # AOP代理接口 ⭐ 新增
    ├── JdkDynamicAopProxy.java                    # JDK动态代理实现 ⭐ 新增
    ├── CglibAopProxy.java                         # CGLIB代理实现 ⭐ 新增
    └── ReflectiveMethodInvocation.java            # 反射方法调用实现 ⭐ 新增
```

### 3.2 类图关系

```
BeanFactory (接口)
    ↑
    │ 实现
    │
AbstractBeanFactory (抽象类)
    │
    │ 继承  ←─── DefaultSingletonBeanRegistry
    │                     ↑
    │                     │ 实现
    │                     │
    │           SingletonBeanRegistry (接口)
    │
    ↓ 继承
    │
AbstractAutowireCapableBeanFactory (抽象类)
```

---

## 四、核心类详解

### 4.1 BeanFactory (接口)
**职责**: IoC 容器的顶层抽象，定义获取 Bean 的基本方法

**核心方法**:
- `Object getBean(String name)`: 根据名称获取 Bean 实例

### 4.2 BeanDefinition (类)
**职责**: 保存 Bean 的元数据信息

**核心属性**:
- `Class beanClass`: Bean 的 Class 类型
- `PropertyValues propertyValues`: Bean 的属性值集合 ⭐ 已实现
- `String scope`: Bean的作用域（singleton/prototype）⭐ 新增
- `boolean singleton`: 是否为单例Bean ⭐ 新增
- `boolean prototype`: 是否为原型Bean ⭐ 新增
- `String initMethodName`: Bean初始化方法名称 ⭐ 已实现
- `String destroyMethodName`: Bean销毁方法名称 ⭐ 已实现

**扩展方向**:
- ✅ 属性值信息 ⭐ 已实现
- ✅ 作用域支持（单例/原型）⭐ 已实现
- ✅ 初始化方法 ⭐ 已实现
- ✅ 销毁方法 ⭐ 已实现
- 依赖关系
- 懒加载支持

### 4.3 SingletonBeanRegistry (接口)
**职责**: 定义单例 Bean 的注册和获取规范

**核心方法**:
- `Object getSingleton(String beanName)`: 获取单例 Bean

### 4.4 DefaultSingletonBeanRegistry (实现类)
**职责**: 单例 Bean 的存储和管理

**核心属性**:
- `Map<String, Object> singletonObjects`: 单例对象缓存池（一级缓存）

**设计思想**: Spring 三级缓存的简化版本，只实现了一级缓存

### 4.5 AbstractBeanFactory (抽象类)
**职责**: 定义获取 Bean 的模板流程

**设计模式**: 模板方法模式
- **模板方法**: `getBean(String name)` - 定义算法骨架
- **钩子方法**: 
  - `createBean()` - 创建 Bean
  - `getBeanDefinition()` - 获取 Bean 定义

**核心流程**:
```
getBean(name)
    ↓
1. 从单例缓存获取 Bean
    ↓ 如果不存在
2. 获取 Bean 定义信息
    ↓
3. 创建 Bean 实例
    ↓
4. 返回 Bean
```

### 4.6 AbstractAutowireCapableBeanFactory (抽象类)
**职责**: 实现 Bean 的创建和自动装配逻辑

**核心方法**:
- `createBean()`: 创建 Bean 实例
- `doCreateBean()`: 执行实际的创建逻辑

**Bean 生命周期** (完整版):
```
1. 实例化：通过反射创建对象 (newInstance)
2. 属性填充：给对象属性赋值 ✅ 已实现
   - 基本属性注入 ✅ 已实现
   - Bean依赖注入 ✅ 已实现
3. 初始化：执行Aware接口、BeanPostProcessor和初始化方法 ✅ 已实现
   - BeanFactoryAware处理 ✅ 已实现
   - BeanPostProcessor前置处理 ✅ 已实现
     * ApplicationContextAware处理 ✅ 已实现
   - 执行Bean初始化方法 ✅ 已实现
     * InitializingBean.afterPropertiesSet() ✅ 已实现
     * 自定义init-method方法 ✅ 已实现
   - BeanPostProcessor后置处理 ✅ 已实现
4. 注册单例：放入单例缓存池（仅单例Bean）✅ 已实现
5. 销毁：ApplicationContext关闭时执行销毁方法（仅单例Bean）✅ 已实现
   - DisposableBean.destroy() ✅ 已实现
   - 自定义destroy-method方法 ✅ 已实现

作用域差异：
- Singleton Bean：创建后注册到缓存，容器关闭时执行销毁
- Prototype Bean：每次获取都创建新实例，不注册到缓存，不执行销毁方法
```

### 4.7 BeanDefinitionRegistry (接口)
**职责**: 定义 Bean 定义的注册规范

**核心方法**:
- `void registerBeanDefinition(String beanName, BeanDefinition beanDefinition)`

**使用场景**: 配置解析阶段，将解析的 Bean 信息注册到容器

### 4.8 DefaultListableBeanFactory (实现类)
**职责**: 完整的 BeanFactory 实现类

**核心属性**:
- `Map<String, BeanDefinition> beanDefinitionMap`: BeanDefinition 存储容器

**职责整合**:
- 继承 AbstractAutowireCapableBeanFactory，获得 Bean 创建能力
- 实现 BeanDefinitionRegistry，获得 Bean 定义注册能力
- 提供完整可用的 IoC 容器实现

### 4.9 InstantiationStrategy (接口) ⭐ 新增
**职责**: 定义 Bean 实例化策略

**核心方法**:
- `Object instantiate(BeanDefinition beanDefinition)`: 实例化 Bean

**设计模式**: 策略模式（Strategy Pattern）
- 定义一系列算法（实例化方式）
- 将每个算法封装起来
- 使算法可以互相替换

**实现类**:
1. **SimpleInstantiationStrategy**: JDK 反射方式
   - 使用 `Constructor.newInstance()` 创建对象
   - 简单直接，性能好
   - 适合普通 Bean

2. **CglibSubclassInstantiationStrategy**: Cglib 动态代理方式
   - 使用 Cglib 生成子类代理
   - 支持方法拦截，为 AOP 做准备
   - 适合需要增强的 Bean

### 4.10 SimpleInstantiationStrategy (实现类) ⭐ 新增
**职责**: 使用 JDK 反射实例化 Bean

**实现方式**:
```java
Constructor constructor = beanClass.getDeclaredConstructor();
return constructor.newInstance();
```

**优点**:
- 无需额外依赖
- 实现简单，性能好
- 适合大部分场景

**缺点**:
- 只能调用无参构造器
- 不支持方法拦截

### 4.11 CglibSubclassInstantiationStrategy (实现类) ⭐ 新增
**职责**: 使用 Cglib 动态代理实例化 Bean

**实现方式**:
```java
Enhancer enhancer = new Enhancer();
enhancer.setSuperclass(beanClass);
enhancer.setCallback(methodInterceptor);
return enhancer.create();
```

**优点**:
- 支持方法拦截（MethodInterceptor）
- 为 AOP 提供基础
- 可以对没有接口的类进行代理

**缺点**:
- 需要引入 Cglib 依赖
- 性能略低于直接反射
- 无法代理 final 类和方法

**应用场景**: Spring AOP 在无接口时默认使用 Cglib 代理

### 4.12 PropertyValue (类) ⭐ 新增
**职责**: 封装Bean的单个属性信息

**核心属性**:
- `String name`: 属性名称
- `Object value`: 属性值

**设计特点**:
- 不可变对象：保证数据一致性
- 类型安全：支持任意类型的属性值
- 简单封装：只包含最基本的属性信息

**使用场景**:
- Bean定义时指定属性值
- 属性注入时传递属性信息
- 配置解析时存储解析出的属性

### 4.13 PropertyValues (类) ⭐ 新增
**职责**: 管理Bean的所有属性值

**核心属性**:
- `List<PropertyValue> propertyValues`: 属性值列表

**核心方法**:
- `addPropertyValue(PropertyValue)`: 添加属性值
- `getPropertyValues()`: 获取所有属性值数组
- `getPropertyValue(String)`: 根据名称查找属性值
- `contains(String)`: 检查是否包含指定属性

**设计特点**:
- 集合管理：使用List存储多个PropertyValue
- 便捷操作：提供增删改查等常用操作
- 不可变返回：防止外部修改内部数据

### 4.14 BeanReference (类) ⭐ 新增
**职责**: 表示Bean之间的依赖引用

**核心属性**:
- `String beanName`: 被引用的Bean名称

**设计特点**:
- 延迟解析：不直接存储Bean实例，而是存储Bean名称
- 循环依赖支持：通过名称引用避免直接对象引用导致的循环依赖问题
- 简单封装：只包含被引用Bean的名称，保持结构简单

**使用场景**:
- Bean属性需要注入其他Bean实例
- 实现Bean之间的依赖关系
- 支持循环依赖的解决

**解析过程**:
1. 创建Bean实例
2. 发现属性是BeanReference类型
3. 调用getBean(beanName)获取被引用的Bean实例
4. 将Bean实例注入到属性中

### 4.15 属性注入机制 ⭐ 新增
**实现位置**: `AbstractAutowireCapableBeanFactory.applyPropertyValues()`

**实现原理**:
1. 遍历BeanDefinition中的所有PropertyValue
2. 判断属性值类型：
   - 基本类型/包装类型/String：直接使用Hutool设置字段值
   - BeanReference：调用getBean()获取Bean实例后设置字段值
3. 支持基本类型、包装类型、String等常见类型的自动转换

**技术特点**:
- 使用Hutool工具库简化反射操作
- 支持字段名自动匹配（忽略大小写）
- 支持Bean之间的依赖注入
- 提供详细的异常信息便于调试

**Bean生命周期更新**:
```
1. 实例化：通过反射创建对象 (newInstance)
2. 属性填充：给对象的属性赋值 ✅ 已实现
   - 基本属性注入 ✅ 已实现
   - Bean依赖注入 ✅ 已实现
3. 初始化：执行BeanPostProcessor和初始化方法 ✅ 已实现
   - BeanPostProcessor前置处理 ✅ 已实现
   - 执行Bean初始化方法 (暂未实现)
   - BeanPostProcessor后置处理 ✅ 已实现
4. 注册单例：放入单例缓存池
```

### 4.16 Resource (接口) ⭐ 新增
**职责**: 定义资源访问的统一规范

**核心方法**:
- `InputStream getInputStream()`: 获取资源的输入流

**设计特点**:
- 统一抽象：为不同类型的资源提供统一的访问接口
- 简单易用：只定义最核心的getInputStream()方法
- 扩展性强：可以轻松添加新的资源类型实现

**实现类**:
- ClassPathResource：classpath下的资源
- FileSystemResource：文件系统中的资源
- UrlResource：网络URL资源

### 4.17 ResourceLoader (接口) ⭐ 新增
**职责**: 定义资源加载的统一规范

**核心方法**:
- `Resource getResource(String location)`: 根据位置字符串获取Resource对象

**设计思想**:
- 策略模式：根据资源位置字符串的不同前缀，选择不同的资源加载策略
- 统一接口：为不同类型的资源提供统一的加载入口
- 扩展性强：可以轻松添加新的资源类型支持

**实现类**:
- DefaultResourceLoader：默认的资源加载器实现

### 4.18 DefaultResourceLoader (实现类) ⭐ 新增
**职责**: ResourceLoader接口的标准实现

**核心属性**:
- `String CLASSPATH_URL_PREFIX = "classpath:"`: classpath资源位置前缀

**实现策略**:
1. "classpath:path" → ClassPathResource
2. 有效URL → UrlResource
3. 其他格式 → FileSystemResource（降级处理）

**技术特点**:
- 策略模式：根据location前缀选择不同的Resource实现策略
- 容错处理：当URL解析失败时，自动降级为文件系统资源
- 简单高效：只处理最常用的资源类型，保持实现简单

### 4.19 ClassPathResource (实现类) ⭐ 新增
**职责**: 用于访问classpath下的资源

**核心属性**:
- `String path`: 资源在classpath中的相对路径

**实现方式**:
```java
InputStream inputStream = this.getClass().getClassLoader().getResourceAsStream(this.path);
```

**技术特点**:
- 简单高效：直接使用ClassLoader.getResourceAsStream()方法
- 路径相对：使用相对于classpath的路径，无需绝对路径
- 异常明确：当资源不存在时提供清晰的异常信息

### 4.20 FileSystemResource (实现类) ⭐ 新增
**职责**: 用于访问文件系统中的资源

**核心属性**:
- `String filePath`: 文件系统路径

**实现方式**:
```java
Path path = new File(this.filePath).toPath();
return Files.newInputStream(path);
```

**技术特点**:
- 路径灵活：支持相对路径和绝对路径
- 异常处理：提供清晰的异常信息，便于调试
- 性能优化：使用NIO.2的Files.newInputStream()方法

### 4.21 UrlResource (实现类) ⭐ 新增
**职责**: 用于访问网络URL资源

**核心属性**:
- `URL url`: 资源URL

**实现方式**:
```java
URLConnection urlConnection = this.url.openConnection();
return urlConnection.getInputStream();
```

**技术特点**:
- 网络访问：支持HTTP、HTTPS等网络协议
- 连接管理：使用URLConnection进行连接管理
- 异常传播：直接传播网络IO异常，便于上层处理

### 4.22 BeanDefinitionReader (接口) ⭐ 新增
**职责**: 定义从各种配置源读取Bean定义信息的规范

**核心方法**:
- `void loadBeanDefinitions(Resource resource)`: 从Resource对象加载Bean定义
- `void loadBeanDefinitions(String location)`: 从资源位置字符串加载Bean定义
- `void loadBeanDefinitions(String[] locations)`: 批量加载Bean定义

**设计特点**:
- 统一抽象：为不同配置格式提供统一的读取接口
- 扩展性强：支持XML、注解、Java配置等多种格式
- 资源管理：集成ResourceLoader进行资源加载

**实现类**:
- AbstractBeanDefinitionReader：抽象基类
- XmlBeanDefinitionReader：XML配置读取器

### 4.23 AbstractBeanDefinitionReader (抽象类) ⭐ 新增
**职责**: Bean定义读取器的抽象基类，提供通用实现

**核心属性**:
- `BeanDefinitionRegistry registry`: Bean定义注册表
- `ResourceLoader resourceLoader`: 资源加载器

**核心方法**:
- `loadBeanDefinitions(String[] locations)`: 批量加载Bean定义
- `setResourceLoader(ResourceLoader)`: 设置资源加载器

**设计模式**: 模板方法模式
- 定义Bean定义读取的基本流程
- 子类实现具体的解析逻辑
- 提供资源加载器的统一管理

### 4.24 XmlBeanDefinitionReader (实现类) ⭐ 新增
**职责**: 从XML配置文件中读取Bean定义信息

**核心常量**:
- `BEAN_ELEMENT = "bean"`: bean元素名称
- `PROPERTY_ELEMENT = "property"`: property元素名称
- `ID_ATTRIBUTE = "id"`: id属性名称
- `CLASS_ATTRIBUTE = "class"`: class属性名称
- `VALUE_ATTRIBUTE = "value"`: value属性名称
- `REF_ATTRIBUTE = "ref"`: ref属性名称

**核心方法**:
- `loadBeanDefinitions(Resource)`: 从Resource加载Bean定义
- `loadBeanDefinitions(String)`: 从位置字符串加载Bean定义
- `doLoadBeanDefinitions(InputStream)`: 执行XML解析逻辑

**技术特点**:
- XML解析：使用Hutool的XmlUtil进行XML解析
- 属性注入：支持value和ref两种属性注入方式
- Bean命名：支持id、name、类名首字母小写三种命名策略
- 异常处理：提供详细的异常信息便于调试

**支持的XML格式**:
```xml
<bean id="beanName" class="com.example.BeanClass">
    <property name="propertyName" value="propertyValue"/>
    <property name="refProperty" ref="otherBean"/>
</bean>
```

### 4.25 BeanFactoryPostProcess (接口) ⭐ 新增
**职责**: 定义BeanFactory后置处理器的规范

**核心方法**:
- `postProcessBeanFactory(ConfigurableListableBeanFactory)`: 在BeanFactory标准初始化后修改BeanDefinition

**设计特点**:
- 扩展点：允许在Bean实例化前修改BeanDefinition
- 执行时机：在BeanDefinition加载完成后，Bean实例化前
- 使用场景：修改Bean属性值、添加Bean定义、实现配置动态修改

**使用示例**:
```java
public class CustomBeanFactoryPostProcessor implements BeanFactoryPostProcess {
    @Override
    public void postProcessBeanFactory(ConfigurableListableBeanFactory beanFactory) {
        BeanDefinition beanDefinition = beanFactory.getBeanDefinition("person");
        PropertyValues propertyValues = beanDefinition.getPropertyValues();
        propertyValues.addPropertyValue(new PropertyValue("name", "ivy"));
    }
}
```

### 4.26 BeanPostProcessor (接口) ⭐ 新增
**职责**: 定义Bean后置处理器的规范

**核心方法**:
- `postProcessBeforeInitialization(Object, String)`: Bean初始化前处理
- `postProcessAfterInitialization(Object, String)`: Bean初始化后处理

**设计特点**:
- 扩展点：允许在Bean实例化后、初始化前后对Bean进行处理
- 执行时机：在Bean实例化后，初始化方法执行前后
- 使用场景：修改Bean属性值、添加代理（AOP基础）、实现Bean增强

**使用示例**:
```java
public class CustomBeanPostProcessor implements BeanPostProcessor {
    @Override
    public Object postProcessBeforeInitialization(Object bean, String beanName) {
        if ("car".equals(beanName)) {
            ((Car) bean).setBrand("lamborghini");
        }
        return bean;
    }
    
    @Override
    public Object postProcessAfterInitialization(Object bean, String beanName) {
        return bean;
    }
}
```

### 4.27 ConfigurableBeanFactory (接口) ⭐ 新增
**职责**: 提供Bean工厂的配置能力

**继承关系**: HierarchicalBeanFactory + SingletonBeanRegistry

**核心方法**:
- `addBeanPostProcessor(BeanPostProcessor)`: 添加Bean后置处理器

**设计特点**:
- 配置能力：提供BeanPostProcessor管理功能
- 层次结构：支持Bean工厂的层次结构
- 单例管理：提供单例Bean的注册和管理

### 4.28 AutowireCapableBeanFactory (接口) ⭐ 新增
**职责**: 提供Bean的自动装配和BeanPostProcessor处理能力

**继承关系**: BeanFactory

**核心方法**:
- `applyBeanPostProcessorsBeforeInitialization(Object, String)`: 执行BeanPostProcessor前置处理
- `applyBeanPostProcessorsAfterInitialization(Object, String)`: 执行BeanPostProcessor后置处理

**设计特点**:
- 自动装配：提供Bean的自动装配支持
- BeanPostProcessor执行：提供BeanPostProcessor的执行能力
- AOP基础：为AOP提供基础支持

### 4.29 ApplicationContext (接口) ⭐ 新增
**职责**: 应用上下文的核心接口，提供Bean管理和资源访问能力

**继承关系**: ListableBeanFactory + HierarchicalBeanFactory + ResourceLoader

**设计特点**:
- 整合功能：整合Bean管理和资源访问能力
- 应用上下文：提供完整的应用上下文功能
- 基础接口：为更高级的配置接口提供基础

**主要功能**:
- Bean的获取和管理
- Bean的列表查询和类型查询
- Bean工厂的层次结构管理
- 资源的加载和访问

### 4.30 ConfigurableApplicationContext (接口) ⭐ 新增
**职责**: 提供应用上下文的配置和管理能力

**继承关系**: ApplicationContext

**核心方法**:
- `refresh() throws Exception`: 刷新应用上下文

**设计特点**:
- 配置能力：提供应用上下文的配置能力
- 生命周期管理：提供完整的生命周期管理
- 高级接口：提供应用上下文的高级功能

**使用场景**:
- 应用启动时
- 手动调用refresh()方法时
- 配置热重载时

### 4.31 AbstractApplicationContext (抽象类) ⭐ 新增
**职责**: 应用上下文的核心抽象类，实现refresh()方法的标准流程

**继承关系**: DefaultResourceLoader + ConfigurableApplicationContext

**核心方法**:
- `refresh()`: 实现应用上下文刷新的标准流程
- `invokeBeanFactoryPostProcessors()`: 执行BeanFactoryPostProcessor
- `registerBeanPostProcessors()`: 注册BeanPostProcessor
- `refreshBeanFactory()`: 抽象方法，子类实现
- `getBeanFactory()`: 抽象方法，子类实现

**设计模式**: 模板方法模式
- 定义refresh()方法的算法骨架
- 子类实现refreshBeanFactory()和getBeanFactory()方法

**刷新流程**:
1. 创建BeanFactory并加载BeanDefinition
2. 执行BeanFactoryPostProcessor（在Bean实例化前）
3. 注册BeanPostProcessor（在Bean实例化前）
4. 预实例化单例Bean

### 4.32 AbstractRefreshableApplicationContext (抽象类) ⭐ 新增
**职责**: 提供可刷新应用上下文的基础实现，支持BeanFactory的重新创建

**继承关系**: AbstractApplicationContext

**核心方法**:
- `refreshBeanFactory()`: 实现BeanFactory的刷新逻辑
- `createBeanFactory()`: 创建BeanFactory实例
- `loadBeanDefinitions()`: 抽象方法，子类实现
- `getBeanFactory()`: 获取BeanFactory实例

**设计特点**:
- 可刷新：每次刷新都会创建新的BeanFactory实例
- 热重载：支持配置更新和热重载
- 灵活加载：提供灵活的BeanDefinition加载机制

### 4.33 AbstractXmlApplicationContext (抽象类) ⭐ 新增
**职责**: 提供基于XML配置的应用上下文基础实现

**继承关系**: AbstractRefreshableApplicationContext

**核心方法**:
- `loadBeanDefinitions()`: 使用XmlBeanDefinitionReader加载XML配置
- `getConfigLocations()`: 抽象方法，子类实现

**设计特点**:
- XML配置：专门处理基于XML配置的应用上下文
- 多文件支持：支持多个XML配置文件的加载
- 资源集成：集成ResourceLoader进行资源加载

**技术特点**:
- 使用XmlBeanDefinitionReader进行XML解析
- 支持classpath、文件系统、URL等资源类型
- 提供灵活的配置文件位置配置

### 4.34 ClassPathXmlApplicationContext (实现类) ⭐ 新增
**职责**: 从classpath加载XML配置文件的应用上下文实现

**继承关系**: AbstractXmlApplicationContext

**核心方法**:
- `ClassPathXmlApplicationContext(String)`: 单个配置文件构造方法
- `ClassPathXmlApplicationContext(String[])`: 多个配置文件构造方法
- `getConfigLocations()`: 获取配置文件位置数组

**设计特点**:
- ClassPath加载：专门从classpath加载XML配置文件
- 自动刷新：构造时自动刷新应用上下文
- 便捷构造：提供便捷的构造方法

**使用示例**:
```java
// 单个配置文件
ApplicationContext context = new ClassPathXmlApplicationContext("spring.xml");

// 多个配置文件
ApplicationContext context = new ClassPathXmlApplicationContext(
    new String[]{"spring.xml", "spring-dao.xml", "spring-service.xml"});

// 获取Bean
UserService userService = context.getBean("userService", UserService.class);
```

### 4.35 InitializingBean (接口) ⭐ 新增
**职责**: 定义Bean初始化回调接口

**核心方法**:
- `afterPropertiesSet() throws BeanException`: 在Bean属性设置完成后执行初始化逻辑

**设计特点**:
- 生命周期回调：提供Bean初始化时的扩展点
- 属性验证：可以在属性设置完成后验证配置的有效性
- 资源初始化：建立外部资源连接、启动后台服务等
- 执行时机：在Bean属性设置完成后，BeanPostProcessor前置处理之后

**使用场景**:
- 验证配置属性的有效性
- 建立与其他服务的连接
- 启动后台线程或定时任务
- 执行任何需要在Bean完全初始化后执行的逻辑

**使用示例**:
```java
public class DatabaseService implements InitializingBean {
    private String url;
    private String username;
    private String password;
    
    @Override
    public void afterPropertiesSet() throws BeanException {
        // 验证数据库连接参数
        if (url == null || username == null || password == null) {
            throw new BeanException("Database connection parameters cannot be null");
        }
        // 建立数据库连接
        initializeConnection();
    }
}
```

### 4.36 DisposableBean (接口) ⭐ 新增
**职责**: 定义Bean销毁回调接口

**核心方法**:
- `destroy() throws Exception`: 在Bean销毁前执行清理逻辑

**设计特点**:
- 生命周期回调：提供Bean销毁时的扩展点
- 资源清理：关闭数据库连接、停止后台线程等
- 执行时机：在ApplicationContext关闭时，按照Bean依赖关系的逆序执行
- 异常处理：清理失败时抛出Exception

**使用场景**:
- 关闭数据库连接
- 停止后台线程或定时任务
- 释放外部资源
- 保存数据到持久化存储

**使用示例**:
```java
public class DatabaseService implements DisposableBean {
    private Connection connection;
    
    @Override
    public void destroy() throws Exception {
        if (connection != null && !connection.isClosed()) {
            connection.close();
            System.out.println("Database connection closed successfully");
        }
    }
}
```

### 4.37 DisposableBeanAdapter (实现类) ⭐ 新增
**职责**: 统一处理Bean销毁逻辑的适配器类

**继承关系**: DisposableBean

**核心属性**:
- `Object bean`: 要销毁的Bean实例
- `String beanName`: Bean名称，用于异常信息
- `String destroyMethodName`: 销毁方法名称（从BeanDefinition中获取）

**核心方法**:
- `destroy()`: 执行Bean的销毁逻辑

**设计特点**:
- 适配器模式：统一不同销毁方式的接口
- 反射调用：支持通过方法名调用销毁方法
- 避免重复：防止同时实现DisposableBean和配置destroy-method时重复调用
- 异常处理：提供详细的异常信息便于调试

**执行逻辑**:
1. 如果Bean实现了DisposableBean接口，先调用其destroy()方法
2. 如果配置了destroy-method且不是"destroy"方法，则通过反射调用指定的销毁方法

**使用场景**:
- 统一管理Bean的销毁逻辑
- 支持多种销毁方式的Bean
- 在ApplicationContext关闭时批量销毁Bean

### 4.38 Aware (接口) ⭐ 新增
**职责**: 标记接口，用于标识Bean能够感知Spring容器的特定组件

**设计特点**:
- 标记接口：本身不定义方法，用于标识Bean的感知能力
- 反向控制：Bean主动感知容器组件，而不是被动等待注入
- 生命周期感知：在Bean生命周期的特定阶段注入容器组件
- 解耦设计：Bean可以访问容器功能而不需要直接依赖容器

**常见的Aware子接口**:
- BeanFactoryAware：感知BeanFactory
- ApplicationContextAware：感知ApplicationContext
- BeanNameAware：感知Bean名称
- ApplicationEventPublisherAware：感知事件发布器

**使用场景**:
- Bean需要访问容器功能（如获取其他Bean）
- Bean需要知道自己的名称或容器信息
- Bean需要发布事件或访问容器服务

### 4.39 BeanFactoryAware (接口) ⭐ 新增
**职责**: 让Bean感知BeanFactory容器的接口

**核心方法**:
- `setBeanFactory(BeanFactory beanFactory)`: 设置BeanFactory容器引用

**设计特点**:
- 容器感知：允许Bean获取对BeanFactory容器的引用
- 动态查找：支持Bean动态获取其他Bean实例
- 执行时机：在Bean属性设置完成后，BeanPostProcessor前置处理之前

**使用场景**:
- Bean需要动态获取其他Bean实例
- Bean需要检查容器中是否存在某个Bean
- Bean需要访问BeanFactory的高级功能
- 实现自定义的Bean查找逻辑

**使用示例**:
```java
public class MyService implements BeanFactoryAware {
    private BeanFactory beanFactory;
    
    @Override
    public void setBeanFactory(BeanFactory beanFactory) throws BeanException {
        this.beanFactory = beanFactory;
    }
    
    public void doSomething() {
        // 动态获取其他Bean
        UserService userService = beanFactory.getBean("userService", UserService.class);
        userService.createUser("张三");
    }
}
```

### 4.40 ApplicationContextAware (接口) ⭐ 新增
**职责**: 让Bean感知ApplicationContext容器的接口

**核心方法**:
- `setApplicationContext(ApplicationContext applicationContext)`: 设置ApplicationContext容器引用

**设计特点**:
- 容器感知：允许Bean获取对ApplicationContext容器的引用
- 丰富功能：ApplicationContext提供了比BeanFactory更丰富的功能
- 执行时机：通过ApplicationContextAwareProcessor在BeanPostProcessor前置处理阶段执行
- 优先级高：优先于BeanFactoryAware执行

**使用场景**:
- Bean需要动态获取其他Bean实例
- Bean需要访问ApplicationContext的高级功能（如国际化、事件发布等）
- Bean需要获取容器中的资源文件
- Bean需要发布应用事件

**使用示例**:
```java
public class MyService implements ApplicationContextAware {
    private ApplicationContext applicationContext;
    
    @Override
    public void setApplicationContext(ApplicationContext applicationContext) throws BeanException {
        this.applicationContext = applicationContext;
    }
    
    public void doSomething() {
        // 动态获取其他Bean
        UserService userService = applicationContext.getBean("userService", UserService.class);
        
        // 发布应用事件
        applicationContext.publishEvent(new CustomEvent("Hello World"));
        
        // 获取资源文件
        Resource resource = applicationContext.getResource("classpath:config.properties");
    }
}
```

### 4.41 ApplicationContextAwareProcessor (实现类) ⭐ 新增
**职责**: 专门处理ApplicationContextAware接口的BeanPostProcessor实现

**继承关系**: BeanPostProcessor

**核心属性**:
- `ApplicationContext applicationContext`: ApplicationContext实例

**核心方法**:
- `postProcessBeforeInitialization()`: Bean初始化前的处理，注入ApplicationContext

**设计特点**:
- 专门的Aware处理器：专门处理ApplicationContextAware接口
- 前置处理：在BeanPostProcessor前置处理阶段执行
- 自动注册：在ApplicationContext初始化时自动注册
- 优先级高：确保ApplicationContextAware优先于BeanFactoryAware执行

**执行逻辑**:
1. 检查Bean是否实现了ApplicationContextAware接口
2. 如果是，调用其setApplicationContext方法注入ApplicationContext实例
3. 返回处理后的Bean

**使用场景**:
- 让Bean感知ApplicationContext容器
- 实现Bean对容器功能的访问
- 支持动态Bean查找和容器操作

### 4.42 Bean作用域支持 ⭐ 新增
**职责**: 支持Bean的不同作用域，包括单例和原型模式

**支持的作用域**:
1. **Singleton（单例）**: 默认作用域
   - 容器中只有一个实例
   - 每次获取都返回同一个对象
   - 创建后注册到单例缓存池
   - 容器关闭时执行销毁方法

2. **Prototype（原型）**: 多实例作用域
   - 每次获取都创建新的实例
   - 容器不管理原型Bean的生命周期
   - 不注册到单例缓存池
   - 不执行销毁方法

**实现机制**:
- BeanDefinition中存储作用域信息（scope、singleton、prototype标志）
- AbstractAutowireCapableBeanFactory根据作用域决定是否注册到缓存
- XmlBeanDefinitionReader解析XML中的scope属性
- 支持默认作用域为singleton

**使用场景**:
- 有状态Bean：需要每次获取新实例
- 线程安全Bean：避免多线程共享状态
- 临时Bean：不需要容器管理生命周期
- 性能敏感Bean：避免单例带来的内存占用

**配置示例**:
```xml
<!-- 单例Bean（默认） -->
<bean id="singletonService" class="com.example.Service" scope="singleton"/>

<!-- 原型Bean -->
<bean id="prototypeService" class="com.example.Service" scope="prototype"/>
```

**注意事项**:
- 原型Bean不支持销毁方法
- 原型Bean不参与循环依赖解决
- 原型Bean每次创建都会执行完整的初始化流程

### 4.43 FactoryBean支持 ⭐ 新增
**职责**: 支持通过FactoryBean创建复杂对象或需要特殊初始化逻辑的对象

**核心接口**:
- `FactoryBean<T>`: FactoryBean接口，定义对象创建规范

**核心方法**:
- `T getObject()`: 获取由FactoryBean创建的对象实例
- `boolean isSingleton()`: 判断创建的对象是否为单例

**实现机制**:
- AbstractBeanFactory中的getObjectForBeanInstance()方法处理FactoryBean
- FactoryBean对象缓存池缓存单例FactoryBean的getObject()结果
- 支持FactoryBean的属性注入和生命周期管理
- 区分FactoryBean本身和FactoryBean创建的对象

**使用场景**:
- 创建复杂对象（如代理对象、连接池等）
- 需要特殊初始化逻辑的对象
- 需要根据配置动态创建的对象
- 需要包装或增强现有对象

**配置示例**:
```xml
<!-- FactoryBean配置 -->
<bean id="car" class="com.example.CarFactoryBean">
    <property name="brand" value="porsche"/>
</bean>
```

**获取方式**:
```java
// 获取FactoryBean创建的对象
Car car = beanFactory.getBean("car", Car.class);

// 获取FactoryBean本身（需要在Bean名称前加"&"）
CarFactoryBean factoryBean = beanFactory.getBean("&car", CarFactoryBean.class);
```

**注意事项**:
- FactoryBean本身也会被注册为Bean
- 单例FactoryBean的getObject()结果会被缓存
- 原型FactoryBean每次调用getObject()创建新实例
- FactoryBean支持完整的Bean生命周期（属性注入、初始化等）

### 4.44 事件系统支持 ⭐ 新增
**职责**: 提供完整的事件发布和监听机制，支持容器生命周期事件和自定义事件

**核心组件**:
- `ApplicationEvent`: 应用事件抽象基类
- `ApplicationListener<E>`: 应用事件监听器接口
- `ApplicationEventPublisher`: 应用事件发布者接口
- `ApplicationEventMulticaster`: 应用事件广播器接口

**事件类型**:
1. **容器生命周期事件**:
   - `ContextRefreshedEvent`: 容器刷新完成事件
   - `ContextClosedEvent`: 容器关闭事件

2. **自定义事件**:
   - 继承自ApplicationEvent的业务事件
   - 支持任意类型的自定义事件

**实现机制**:
- 观察者模式：事件发布者发布事件，监听器监听并处理事件
- 泛型支持：通过泛型确保类型安全的事件监听
- 反射匹配：通过反射检查监听器是否支持特定事件类型
- 同步广播：事件广播过程是同步的，监听器按顺序执行

**使用场景**:
- 容器生命周期监听（启动、关闭等）
- 业务事件发布（用户注册、订单创建等）
- 系统状态变化通知
- 组件间松耦合通信

**配置示例**:
```xml
<!-- 事件监听器配置 -->
<bean class="com.example.ContextRefreshedEventListener"/>
<bean class="com.example.CustomEventListener"/>
<bean class="com.example.ContextClosedEventListener"/>
```

**使用示例**:
```java
// 发布自定义事件
applicationContext.publishEvent(new CustomEvent(applicationContext));

// 监听器实现
public class CustomEventListener implements ApplicationListener<CustomEvent> {
    @Override
    public void onApplicationEvent(CustomEvent event) {
        System.out.println("收到自定义事件: " + event);
    }
}
```

**技术特点**:
- 类型安全：通过泛型确保事件类型匹配
- 自动注册：监听器通过XML配置自动注册
- 异常隔离：单个监听器异常不影响其他监听器
- 扩展性强：支持任意类型的自定义事件

### 4.45 AOP支持 ⭐ 新增
**职责**: 提供完整的面向切面编程支持，包括动态代理、切点匹配、通知执行等功能

**核心组件**:
- `AdvisedSupport`: AOP通知支持类，封装代理所需的所有信息
- `TargetSource`: 目标对象源，封装被代理的目标对象信息
- `MethodMatcher`: 方法匹配器接口，判断哪些方法需要被代理
- `ClassFilter`: 类过滤器接口，判断哪些类需要被代理
- `Pointcut`: 切点接口，定义切点的抽象概念

**通知类型**:
- `BeforeAdvice`: 前置通知接口，在目标方法执行前执行
- `AfterAdvice`: 后置通知接口，在目标方法执行后执行
- `AfterReturningAdvice`: 返回后通知接口，在目标方法正常返回后执行
- `ThrowsAdvice`: 异常通知接口，在目标方法抛出异常时执行
- `AroundAdvice`: 环绕通知接口，在目标方法执行前后都执行

**通知器**:
- `Advisor`: 通知器接口，封装通知的抽象概念
- `PointcutAdvisor`: 切点通知器接口，结合切点和通知
- `AspectJExpressionPointcutAdvisor`: AspectJ表达式切点通知器实现

**代理实现**:
1. **JDK动态代理**:
   - `JdkDynamicAopProxy`: 基于接口的AOP代理
   - 适用于有接口的目标类
   - 只能代理接口中定义的方法

2. **CGLIB代理**:
   - `CglibAopProxy`: 基于继承的AOP代理
   - 适用于没有接口的目标类
   - 可以代理类中的所有方法

3. **代理工厂**:
   - `ProxyFactory`: 代理工厂类，自动选择合适的代理实现
   - 支持JDK动态代理和CGLIB代理的自动选择

**切点匹配**:
- `AspectJExpressionPointcut`: AspectJ表达式切点实现
- 支持execution表达式语法
- 提供类级别和方法级别的匹配
- 支持接口继承匹配（实现类可以匹配接口的切点表达式）

**方法调用**:
- `ReflectiveMethodInvocation`: 反射方法调用实现
- 封装方法调用的上下文信息
- 支持拦截器链的执行

**通知拦截器**:
- `MethodBeforeAdviceInterceptor`: 前置通知拦截器
- `MethodAfterAdviceInterceptor`: 后置通知拦截器
- `MethodAfterReturningAdviceInterceptor`: 返回后通知拦截器
- `MethodThrowsAdviceInterceptor`: 异常通知拦截器
- `MethodAroundAdviceInterceptor`: 环绕通知拦截器
- `CombineAdviceInterceptor`: 组合通知拦截器，支持多种通知类型组合

**使用场景**:
- 日志记录：在方法执行前后记录日志
- 性能监控：统计方法执行时间
- 事务管理：在方法执行前后处理事务
- 安全检查：在方法执行前进行权限验证
- 缓存处理：在方法执行前后处理缓存

**基本使用示例**:
```java
// 1. 创建目标对象
WorldService worldService = new WorldServiceImpl();

// 2. 配置AOP信息
AdvisedSupport advisedSupport = new AdvisedSupport();
TargetSource targetSource = new TargetSource(worldService);
WorldServiceInterceptor methodInterceptor = new WorldServiceInterceptor();
MethodMatcher methodMatcher = new AspectJExpressionPointcut("execution(* org.winterframework.test.service.WorldService.explode(..))").getMethodMatcher();

advisedSupport.setTargetSource(targetSource);
advisedSupport.setMethodInterceptor(methodInterceptor);
advisedSupport.setMethodMatcher(methodMatcher);

// 3. 创建代理对象
WorldService proxy = (WorldService) new ProxyFactory(advisedSupport).getProxy();
proxy.explode();
```

**使用AspectJ表达式切点通知器**:
```java
// 1. 创建切点通知器
AspectJExpressionPointcutAdvisor advisor = new AspectJExpressionPointcutAdvisor();
advisor.setExpression("execution(* org.winterframework.test.service.WorldService.explode(..))");
advisor.setAdvice(new MethodBeforeAdviceInterceptor(new WorldServiceBeforeAdvice()));

// 2. 配置AOP信息
AdvisedSupport advisedSupport = new AdvisedSupport();
advisedSupport.setTargetSource(new TargetSource(worldService));
advisedSupport.setMethodInterceptor((MethodInterceptor) advisor.getAdvice());
advisedSupport.setMethodMatcher(advisor.getPointcut().getMethodMatcher());

// 3. 创建代理对象
WorldService proxy = (WorldService) new ProxyFactory(advisedSupport).getProxy();
```

**通知类型使用示例**:
```java
// 前置通知
public class MyBeforeAdvice implements MethodBeforeAdvice {
    @Override
    public void before(Method method, Object[] args, Object target) throws Throwable {
        System.out.println("方法执行前：" + method.getName());
    }
}

// 环绕通知
public class MyAroundAdvice implements MethodAroundAdvice {
    @Override
    public Object around(MethodInvocation invocation) throws Throwable {
        System.out.println("方法执行前");
        Object result = invocation.proceed();
        System.out.println("方法执行后");
        return result;
    }
}

// 组合通知
CombineAdviceInterceptor combineInterceptor = new CombineAdviceInterceptor();
combineInterceptor.setBeforeAdviceInterceptor(new MethodBeforeAdviceInterceptor(new MyBeforeAdvice()));
combineInterceptor.setAroundAdviceInterceptor(new MethodAroundAdviceInterceptor(new MyAroundAdvice()));
```

**技术特点**:
- 双重代理支持：同时支持JDK动态代理和CGLIB代理
- 切点表达式：支持AspectJ表达式语法进行精确匹配
- 方法拦截：通过MethodInterceptor实现方法拦截
- 类型安全：通过泛型确保代理对象的类型安全
- 性能优化：CGLIB使用MethodProxy进行方法调用，性能更好
- 通知组合：支持多种通知类型的组合使用
- 接口继承匹配：实现类可以匹配接口的切点表达式

**AOP执行流程**:
1. 创建代理对象（JDK或CGLIB）
2. 拦截方法调用
3. 检查方法是否匹配切点条件
4. 如果匹配，执行MethodInterceptor
5. 如果不匹配，直接调用目标方法
6. 返回方法执行结果

**通知执行顺序**:
- 环绕通知优先级最高，如果配置了环绕通知，其他通知不会执行
- 否则按以下顺序：前置通知 → 目标方法 → 后置通知/返回后通知/异常通知

---

## 五、核心流程分析

### 5.1 Bean 获取流程

```
用户调用 getBean("userService")
         ↓
1. 进入 AbstractBeanFactory.getBean()
         ↓
2. 调用 getBeanDefinition("userService") 获取 Bean 定义
         ↓
3. 判断Bean作用域
         ↓
    【Singleton Bean】
         ↓
    调用 getSingleton("userService") 查询缓存
         ↓
    【缓存命中】直接返回 Bean
         ↓
    【缓存未命中】继续执行
         ↓
    【Prototype Bean】跳过缓存查询
         ↓
4. 调用 createBean("userService", beanDefinition)
         ↓
5. 进入 AbstractAutowireCapableBeanFactory.doCreateBean()
         ↓
6. 调用 createBeanInstance(beanDefinition)
         ↓
7. 委托给 InstantiationStrategy.instantiate() 实例化
         ↓ 
    【SimpleInstantiationStrategy】使用 JDK 反射创建实例
    或
    【CglibSubclassInstantiationStrategy】使用 Cglib 创建代理
         ↓
8. 调用 applyPropertyValues() 进行属性注入 ⭐ 已实现
         ↓
    【遍历PropertyValues】
         ↓
    【判断属性值类型】
         ↓
    【基本类型/String】使用Hutool直接设置字段值
         ↓
    【BeanReference】调用getBean()获取Bean实例后设置字段值 ⭐ 已实现
         ↓
9. 调用 initializeBean() 进行Bean初始化 ⭐ 已实现
         ↓
    【BeanFactoryAware处理】✅ 已实现
         ↓
    【BeanPostProcessor前置处理】applyBeanPostProcessorsBeforeInitialization() ⭐ 已实现
         ↓
    【ApplicationContextAware处理】✅ 已实现
         ↓
    【执行Bean初始化方法】invokeInitMethods() ✅ 已实现
         ↓
    【InitializingBean.afterPropertiesSet()】✅ 已实现
         ↓
    【自定义init-method方法】✅ 已实现
         ↓
    【BeanPostProcessor后置处理】applyBeanPostProcessorsAfterInitialization() ⭐ 已实现
         ↓
10. 根据作用域决定是否注册到缓存
         ↓
    【Singleton Bean】调用 addSingleton("userService", bean) 注册到缓存
         ↓
    【Prototype Bean】跳过缓存注册
         ↓
11. 调用 getObjectForBeanInstance() 处理FactoryBean
         ↓
    【普通Bean】直接返回原实例
         ↓
    【FactoryBean】调用 getObject() 获取实际对象
         ↓
    【单例FactoryBean】缓存getObject()结果
         ↓
    【原型FactoryBean】每次调用getObject()创建新实例
         ↓
12. 返回最终对象实例
```

### 5.2 Bean 注册流程 (XML配置) ⭐ 新增

```
用户调用 loadBeanDefinitions("classpath:spring.xml")
         ↓
1. 进入 XmlBeanDefinitionReader.loadBeanDefinitions(String)
         ↓
2. 通过 ResourceLoader 加载 XML 资源
         ↓
3. 调用 doLoadBeanDefinitions(InputStream) 解析XML
         ↓
4. 使用 XmlUtil.readXML() 解析XML文档
         ↓
5. 遍历XML节点，查找 <bean> 元素
         ↓
6. 解析bean属性：id、name、class
         ↓
7. 解析property子元素：name、value、ref
         ↓
8. 创建 BeanDefinition 对象
         ↓
9. 创建 PropertyValue 对象并添加到BeanDefinition
         ↓
10. 检查Bean名称是否重复
         ↓
11. 调用 registerBeanDefinition() 注册到容器
         ↓
12. 完成Bean定义注册
```

### 5.3 ApplicationContext刷新流程 ⭐ 新增

```
用户调用 new ClassPathXmlApplicationContext("spring.xml")
         ↓
1. 调用 refresh() 方法
         ↓
2. 调用 refreshBeanFactory() 创建BeanFactory
         ↓
3. 调用 loadBeanDefinitions() 加载XML配置
         ↓
4. 调用 invokeBeanFactoryPostProcessors() 执行BeanFactoryPostProcessor
         ↓
5. 调用 registerBeanPostProcessors() 注册BeanPostProcessor
         ↓
6. 调用 preInstantiateSingletons() 预实例化单例Bean
         ↓
7. ApplicationContext准备就绪
```

### 5.4 完整的Bean生命周期 (包含XML配置) ⭐ 新增

```
配置阶段：
XML文件 → XmlBeanDefinitionReader → BeanDefinition → BeanDefinitionRegistry

运行阶段：
getBean(name) → 从缓存获取 → 创建Bean实例 → 属性注入 → 注册单例 → 返回Bean
```

---

## 六、设计模式应用

### 6.1 模板方法模式
- **应用**: AbstractBeanFactory
- **模板方法**: getBean()
- **钩子方法**: createBean(), getBeanDefinition()
- **优势**: 定义算法骨架，细节由子类实现

### 6.2 策略模式 ⭐ 新增
- **应用**: InstantiationStrategy 及其实现类
- **策略接口**: InstantiationStrategy
- **具体策略**: 
  - SimpleInstantiationStrategy（JDK 反射）
  - CglibSubclassInstantiationStrategy（Cglib 代理）
- **优势**: 
  - 算法可以互相替换
  - 符合开闭原则（扩展新策略不需修改现有代码）
  - 客户端可以动态切换策略

**策略模式 UML**:
```
                  ┌─────────────────────────────┐
                  │   InstantiationStrategy     │
                  │         (策略接口)            │
                  └─────────────┬───────────────┘
                                │
                ┌───────────────┴───────────────┐
                │                               │
    ┌───────────▼──────────┐       ┌───────────▼──────────┐
    │ SimpleInstantiation  │       │ CglibSubclass        │
    │    Strategy          │       │ InstantiationStrategy│
    │   (JDK反射策略)       │       │   (Cglib代理策略)     │
    └──────────────────────┘       └──────────────────────┘
```

### 6.3 工厂模式
- **应用**: BeanFactory 及其实现类
- **作用**: 封装对象创建逻辑

### 6.4 单例模式
- **应用**: DefaultSingletonBeanRegistry
- **实现**: 通过 HashMap 缓存单例对象

---

## 七、与 Spring 的对比

| 功能 | Winter Framework | Spring Framework |
|------|-----------------|------------------|
| 单例缓存 | 一级缓存 | 三级缓存（解决循环依赖） |
| Bean 创建 | 反射 + Cglib 动态代理 ✅ | 反射 + CGLIB 动态代理 |
| 实例化策略 | 支持策略切换 ✅ | 支持多种实例化方式 |
| 属性注入 | 基本属性注入 ✅ | 构造器注入、Setter注入、字段注入 |
| 依赖注入 | Bean依赖注入 ✅ | Bean之间的依赖关系注入 |
| 资源管理 | 基本Resource支持 ✅ | 完整的Resource体系 |
| AOP | 基础AOP支持 ⭐ | 完整的AOP支持 |
| 配置方式 | 手动注册 | XML、注解、Java Config |
| 作用域 | 仅单例 | 单例、原型、请求、会话等 |

---

## 八、扩展方向

### 8.1 已实现
✅ Bean 的基本创建和缓存  
✅ 单例模式支持  
✅ 模板方法模式设计  
✅ 策略模式实现（实例化策略） ⭐ 已实现  
✅ JDK 反射实例化 ⭐ 已实现  
✅ Cglib 动态代理实例化 ⭐ 已实现  
✅ BeanDefinition 注册机制 ⭐ 已实现  
✅ 完整的 BeanFactory 实现（DefaultListableBeanFactory） ⭐ 已实现  
✅ **属性注入功能** ⭐ 已实现  
✅ **PropertyValue 和 PropertyValues** ⭐ 已实现  
✅ **Hutool 工具库集成** ⭐ 已实现
✅ **Bean依赖注入** ⭐ 已实现
✅ **Resource资源管理** ⭐ 已实现
✅ **XML配置解析** ⭐ 已实现
✅ **BeanFactoryPostProcessor支持** ⭐ 已实现
✅ **BeanPostProcessor支持** ⭐ 已实现
✅ **ApplicationContext支持** ⭐ 已实现
✅ **ClassPathXmlApplicationContext实现** ⭐ 已实现
✅ **Bean初始化和销毁方法支持** ⭐ 已实现
✅ **Aware接口支持** ⭐ 已实现
✅ **Bean作用域支持（Singleton/Prototype）** ⭐ 已实现
✅ **FactoryBean支持** ⭐ 新增
✅ **事件系统支持** ⭐ 新增
✅ **AOP支持** ⭐ 新增
✅ **完整的AOP功能**: 切面编程、通知类型、切面组合 ⭐ 已实现

### 8.2 待实现  
🔲 **注解配置**: @Component、@Autowired等注解支持  
🔲 **循环依赖**: 实现三级缓存  
🔲 **懒加载**: 支持Bean的延迟初始化
🔲 **更多作用域**: request、session等Web作用域
🔲 **事务管理**: 声明式事务支持
🔲 **MVC支持**: Web MVC框架支持

---

## 九、使用示例

### 9.1 基本使用

```java
// 1. 创建 BeanFactory
DefaultListableBeanFactory beanFactory = new DefaultListableBeanFactory();

// 2. 注册 Bean 定义
BeanDefinition beanDefinition = new BeanDefinition(UserService.class);
beanFactory.registerBeanDefinition("userService", beanDefinition);

// 3. 获取 Bean
UserService userService = (UserService) beanFactory.getBean("userService");

// 4. 再次获取（从缓存返回）
UserService userService2 = (UserService) beanFactory.getBean("userService");

// userService == userService2  (单例)
```

### 9.2 使用不同的实例化策略

```java
// 创建 BeanFactory
DefaultListableBeanFactory beanFactory = new DefaultListableBeanFactory();

// 场景1：使用默认策略（JDK 反射）
beanFactory.registerBeanDefinition("service1", 
    new BeanDefinition(UserService.class));
Object bean1 = beanFactory.getBean("service1");
// bean1 是通过 JDK 反射创建的普通对象

// 场景2：切换为 Cglib 策略
beanFactory.setInstantiationStrategy(new CglibSubclassInstantiationStrategy());
beanFactory.registerBeanDefinition("service2", 
    new BeanDefinition(OrderService.class));
Object bean2 = beanFactory.getBean("service2");
// bean2 是通过 Cglib 创建的代理对象
```

### 9.3 使用XML配置文件 ⭐ 新增

```java
// 1. 创建BeanFactory和XML读取器
DefaultListableBeanFactory beanFactory = new DefaultListableBeanFactory();
XmlBeanDefinitionReader reader = new XmlBeanDefinitionReader(beanFactory);

// 2. 从XML文件加载Bean定义
reader.loadBeanDefinitions("classpath:spring.xml");

// 3. 获取Bean实例
UserService userService = (UserService) beanFactory.getBean("userService");
OrderService orderService = (OrderService) beanFactory.getBean("orderService");
```

**XML配置文件示例 (spring.xml)**:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- 基本Bean定义 -->
    <bean id="userService" class="com.example.UserService">
        <property name="name" value="derek"/>
        <property name="age" value="25"/>
    </bean>

    <!-- Bean依赖注入 -->
    <bean id="orderService" class="com.example.OrderService">
        <property name="userService" ref="userService"/>
        <property name="orderId" value="12345"/>
    </bean>

</beans>
```

### 9.4 属性注入示例 ⭐ 新增

```java
// Person类
public class Person {
    private String name;
    private int age;
    private Car car;
    
    // getter/setter方法...
}

// Car类
public class Car {
    private String brand;
    private String model;
    
    // getter/setter方法...
}

// XML配置
<bean id="person" class="com.example.Person">
    <property name="name" value="derek"/>
    <property name="age" value="25"/>
    <property name="car" ref="car"/>
</bean>

<bean id="car" class="com.example.Car">
    <property name="brand" value="porsche"/>
    <property name="model" value="911"/>
</bean>

// 使用
Person person = (Person) beanFactory.getBean("person");
System.out.println(person.getName()); // derek
System.out.println(person.getCar().getBrand()); // porsche
```

### 9.5 BeanFactoryPostProcessor和BeanPostProcessor示例 ⭐ 新增

```java
// 1. 创建BeanFactory和XML读取器
DefaultListableBeanFactory beanFactory = new DefaultListableBeanFactory();
XmlBeanDefinitionReader reader = new XmlBeanDefinitionReader(beanFactory);

// 2. 从XML文件加载Bean定义
reader.loadBeanDefinitions("classpath:spring.xml");

// 3. 使用BeanFactoryPostProcessor修改BeanDefinition
CustomBeanFactoryPostProcessor beanFactoryPostProcessor = new CustomBeanFactoryPostProcessor();
beanFactoryPostProcessor.postProcessBeanFactory(beanFactory);

// 4. 添加BeanPostProcessor
CustomerBeanPostProcessor beanPostProcessor = new CustomerBeanPostProcessor();
beanFactory.addBeanPostProcessor(beanPostProcessor);

// 5. 获取Bean实例
Person person = (Person) beanFactory.getBean("person");
Car car = (Car) beanFactory.getBean("car");

// 验证结果
System.out.println(person.getName()); // ivy (被BeanFactoryPostProcessor修改)
System.out.println(car.getBrand()); // lamborghini (被BeanPostProcessor修改)
```

### 9.6 ApplicationContext使用示例 ⭐ 新增

```java
// 1. 使用ClassPathXmlApplicationContext（推荐方式）
ApplicationContext context = new ClassPathXmlApplicationContext("spring.xml");

// 2. 获取Bean实例
UserService userService = context.getBean("userService", UserService.class);
OrderService orderService = context.getBean("orderService", OrderService.class);

// 3. 使用Bean
userService.createUser("张三");
orderService.createOrder("ORDER-001");

// 4. 多个配置文件
ApplicationContext context2 = new ClassPathXmlApplicationContext(
    new String[]{"spring.xml", "spring-dao.xml", "spring-service.xml"});

// 5. 获取所有同类型Bean
Map<String, UserService> userServices = context.getBeansOfType(UserService.class);
```

### 9.7 Bean初始化和销毁方法使用示例 ⭐ 新增

```java
// 1. 实现InitializingBean和DisposableBean接口
public class DatabaseService implements InitializingBean, DisposableBean {
    private String url;
    private String username;
    private String password;
    private Connection connection;
    
    // getter/setter方法...
    
    @Override
    public void afterPropertiesSet() throws BeanException {
        // 验证配置参数
        if (url == null || username == null || password == null) {
            throw new BeanException("Database connection parameters cannot be null");
        }
        // 建立数据库连接
        connection = DriverManager.getConnection(url, username, password);
        System.out.println("Database connection initialized: " + url);
    }
    
    @Override
    public void destroy() throws Exception {
        if (connection != null && !connection.isClosed()) {
            connection.close();
            System.out.println("Database connection closed");
        }
    }
}

// 2. XML配置方式
<bean id="databaseService" class="com.example.DatabaseService">
    <property name="url" value="jdbc:mysql://localhost:3306/test"/>
    <property name="username" value="root"/>
    <property name="password" value="password"/>
</bean>

// 3. 使用自定义初始化方法
public class FileService {
    private String filePath;
    private FileWriter writer;
    
    // getter/setter方法...
    
    public void initFile() throws IOException {
        writer = new FileWriter(filePath);
        System.out.println("File service initialized: " + filePath);
    }
    
    public void cleanupFile() throws IOException {
        if (writer != null) {
            writer.close();
            System.out.println("File service cleaned up");
        }
    }
}

// 4. XML配置自定义方法
<bean id="fileService" class="com.example.FileService" 
      init-method="initFile" destroy-method="cleanupFile">
    <property name="filePath" value="/tmp/log.txt"/>
</bean>

// 5. 使用ApplicationContext
ApplicationContext context = new ClassPathXmlApplicationContext("spring.xml");

// 获取Bean（会自动调用初始化方法）
DatabaseService dbService = context.getBean("databaseService", DatabaseService.class);
FileService fileService = context.getBean("fileService", FileService.class);

// 使用Bean
dbService.queryData();
fileService.writeData("Hello World");

// 关闭ApplicationContext（会自动调用销毁方法）
((ConfigurableApplicationContext) context).close();
```

**输出结果**:
```
Database connection initialized: jdbc:mysql://localhost:3306/test
File service initialized: /tmp/log.txt
执行bean[databaseService]的初始化方法
执行bean[fileService]的初始化方法
Database connection closed
File service cleaned up
```

### 9.8 Bean作用域使用示例 ⭐ 新增

```java
// 1. 定义测试Bean类
public class CounterService {
    private int count = 0;
    private String name;
    
    public CounterService() {
        System.out.println("CounterService实例被创建: " + this.hashCode());
    }
    
    public void increment() {
        count++;
        System.out.println(name + " 计数: " + count);
    }
    
    public int getCount() {
        return count;
    }
    
    public void setName(String name) {
        this.name = name;
    }
    
    public String getName() {
        return name;
    }
}

// 2. XML配置不同作用域的Bean
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- 单例Bean（默认作用域） -->
    <bean id="singletonCounter" class="com.example.CounterService" scope="singleton">
        <property name="name" value="单例计数器"/>
    </bean>

    <!-- 原型Bean -->
    <bean id="prototypeCounter" class="com.example.CounterService" scope="prototype">
        <property name="name" value="原型计数器"/>
    </bean>

</beans>

// 3. 测试不同作用域的行为
public class ScopeTest {
    public static void main(String[] args) {
        ApplicationContext context = new ClassPathXmlApplicationContext("scope-test.xml");
        
        System.out.println("=== 单例Bean测试 ===");
        // 获取单例Bean多次
        CounterService singleton1 = context.getBean("singletonCounter", CounterService.class);
        CounterService singleton2 = context.getBean("singletonCounter", CounterService.class);
        
        System.out.println("singleton1 == singleton2: " + (singleton1 == singleton2));
        singleton1.increment(); // 计数: 1
        singleton2.increment(); // 计数: 2 (同一个实例)
        
        System.out.println("\n=== 原型Bean测试 ===");
        // 获取原型Bean多次
        CounterService prototype1 = context.getBean("prototypeCounter", CounterService.class);
        CounterService prototype2 = context.getBean("prototypeCounter", CounterService.class);
        
        System.out.println("prototype1 == prototype2: " + (prototype1 == prototype2));
        prototype1.increment(); // 计数: 1
        prototype2.increment(); // 计数: 1 (不同实例)
    }
}
```

**输出结果**:
```
CounterService实例被创建: 1234567890
=== 单例Bean测试 ===
singleton1 == singleton2: true
单例计数器 计数: 1
单例计数器 计数: 2

=== 原型Bean测试 ===
CounterService实例被创建: 2345678901
CounterService实例被创建: 3456789012
prototype1 == prototype2: false
原型计数器 计数: 1
原型计数器 计数: 1
```

**作用域对比**:

| 特性 | Singleton | Prototype |
|------|-----------|-----------|
| 实例数量 | 容器中只有一个 | 每次获取都创建新实例 |
| 内存占用 | 低（共享实例） | 高（多个实例） |
| 线程安全 | 需要自行保证 | 天然线程安全 |
| 生命周期管理 | 容器管理 | 不管理 |
| 销毁方法 | 支持 | 不支持 |
| 适用场景 | 无状态服务 | 有状态对象 |

### 9.9 Aware接口使用示例 ⭐ 已实现

```java
// 1. 实现BeanFactoryAware接口
public class UserService implements BeanFactoryAware {
    private BeanFactory beanFactory;
    private String serviceName;
    
    @Override
    public void setBeanFactory(BeanFactory beanFactory) throws BeanException {
        this.beanFactory = beanFactory;
        System.out.println("UserService感知到BeanFactory容器");
    }
    
    public void createUser(String name) {
        System.out.println("创建用户: " + name);
        // 动态获取其他Bean
        OrderService orderService = beanFactory.getBean("orderService", OrderService.class);
        orderService.createOrder("ORDER-" + name);
    }
}

// 2. 实现ApplicationContextAware接口
public class OrderService implements ApplicationContextAware {
    private ApplicationContext applicationContext;
    
    @Override
    public void setApplicationContext(ApplicationContext applicationContext) throws BeanException {
        this.applicationContext = applicationContext;
        System.out.println("OrderService感知到ApplicationContext容器");
    }
    
    public void createOrder(String orderId) {
        System.out.println("创建订单: " + orderId);
        // 获取资源文件
        Resource resource = applicationContext.getResource("classpath:order.properties");
        System.out.println("订单配置资源: " + resource);
    }
}

// 3. 同时实现多个Aware接口
public class ProductService implements ApplicationContextAware, BeanFactoryAware {
    private ApplicationContext applicationContext;
    private BeanFactory beanFactory;
    
    @Override
    public void setApplicationContext(ApplicationContext applicationContext) throws BeanException {
        this.applicationContext = applicationContext;
        System.out.println("ProductService感知到ApplicationContext容器");
    }
    
    @Override
    public void setBeanFactory(BeanFactory beanFactory) throws BeanException {
        this.beanFactory = beanFactory;
        System.out.println("ProductService感知到BeanFactory容器");
    }
    
    public void createProduct(String productName) {
        System.out.println("创建产品: " + productName);
        
        // 使用ApplicationContext获取Bean
        UserService userService = applicationContext.getBean("userService", UserService.class);
        userService.createUser("产品管理员");
        
        // 使用BeanFactory获取Bean
        OrderService orderService = beanFactory.getBean("orderService", OrderService.class);
        orderService.createOrder("PRODUCT-" + productName);
    }
}

// 4. XML配置
<bean id="userService" class="com.example.UserService">
    <property name="serviceName" value="用户服务"/>
</bean>

<bean id="orderService" class="com.example.OrderService"/>

<bean id="productService" class="com.example.ProductService"/>

// 5. 使用ApplicationContext
ApplicationContext context = new ClassPathXmlApplicationContext("spring.xml");

// 获取Bean（会自动处理Aware接口）
UserService userService = context.getBean("userService", UserService.class);
OrderService orderService = context.getBean("orderService", OrderService.class);
ProductService productService = context.getBean("productService", ProductService.class);

// 使用Bean
userService.createUser("张三");
orderService.createOrder("ORDER-001");
productService.createProduct("iPhone 15");
```

**输出结果**:
```
UserService感知到BeanFactory容器
OrderService感知到ApplicationContext容器
ProductService感知到ApplicationContext容器
ProductService感知到BeanFactory容器
执行bean[userService]的初始化方法
执行bean[orderService]的初始化方法
执行bean[productService]的初始化方法
创建用户: 张三
OrderService感知到ApplicationContext容器
创建订单: ORDER-张三
订单配置资源: class path resource [order.properties]
创建用户: 张三
OrderService感知到ApplicationContext容器
创建订单: ORDER-张三
订单配置资源: class path resource [order.properties]
创建产品: iPhone 15
创建用户: 产品管理员
OrderService感知到ApplicationContext容器
创建订单: ORDER-产品管理员
订单配置资源: class path resource [order.properties]
创建订单: PRODUCT-iPhone 15
订单配置资源: class path resource [order.properties]
```

**注意事项**:
- BeanFactoryAware在BeanPostProcessor前置处理之前执行
- ApplicationContextAware通过ApplicationContextAwareProcessor在BeanPostProcessor前置处理阶段执行
- 如果同时实现多个Aware接口，会按顺序执行
- ApplicationContextAware优先级高于BeanFactoryAware

### 9.10 AOP使用示例 ⭐ 新增

**基本AOP使用**:
```java
// 1. 创建目标对象
WorldService worldService = new WorldServiceImpl();

// 2. 配置AOP信息
AdvisedSupport advisedSupport = new AdvisedSupport();
TargetSource targetSource = new TargetSource(worldService);
WorldServiceInterceptor methodInterceptor = new WorldServiceInterceptor();
MethodMatcher methodMatcher = new AspectJExpressionPointcut("execution(* org.winterframework.test.service.WorldService.explode(..))").getMethodMatcher();

advisedSupport.setTargetSource(targetSource);
advisedSupport.setMethodInterceptor(methodInterceptor);
advisedSupport.setMethodMatcher(methodMatcher);

// 3. 创建代理对象
WorldService proxy = (WorldService) new ProxyFactory(advisedSupport).getProxy();
proxy.explode();
```

**使用AspectJ表达式切点通知器**:
```java
// 1. 创建切点通知器
AspectJExpressionPointcutAdvisor advisor = new AspectJExpressionPointcutAdvisor();
advisor.setExpression("execution(* org.winterframework.test.service.WorldService.explode(..))");
advisor.setAdvice(new MethodBeforeAdviceInterceptor(new WorldServiceBeforeAdvice()));

// 2. 配置AOP信息
AdvisedSupport advisedSupport = new AdvisedSupport();
advisedSupport.setTargetSource(new TargetSource(worldService));
advisedSupport.setMethodInterceptor((MethodInterceptor) advisor.getAdvice());
advisedSupport.setMethodMatcher(advisor.getPointcut().getMethodMatcher());

// 3. 创建代理对象
WorldService proxy = (WorldService) new ProxyFactory(advisedSupport).getProxy();
```

**通知类型使用示例**:
```java
// 前置通知
public class MyBeforeAdvice implements MethodBeforeAdvice {
    @Override
    public void before(Method method, Object[] args, Object target) throws Throwable {
        System.out.println("方法执行前：" + method.getName());
    }
}

// 后置通知
public class MyAfterAdvice implements MethodAfterAdvice {
    @Override
    public void after(Method method, Object[] args, Object target) throws Throwable {
        System.out.println("方法执行后：" + method.getName());
    }
}

// 返回后通知
public class MyAfterReturningAdvice implements AfterReturningAdvice {
    @Override
    public void afterReturning(Object returnValue, Method method, Object[] args, Object target) throws Throwable {
        System.out.println("方法正常返回：" + returnValue);
    }
}

// 异常通知
public class MyThrowsAdvice implements MethodThrowsAdvice {
    @Override
    public void throwsHandle(Throwable throwable, Method method, Object[] args, Object target) {
        System.out.println("方法抛出异常：" + throwable.getMessage());
    }
}

// 环绕通知
public class MyAroundAdvice implements MethodAroundAdvice {
    @Override
    public Object around(MethodInvocation invocation) throws Throwable {
        System.out.println("方法执行前");
        Object result = invocation.proceed();
        System.out.println("方法执行后");
        return result;
    }
}
```

**组合通知使用示例**:
```java
// 创建组合通知拦截器
CombineAdviceInterceptor combineInterceptor = new CombineAdviceInterceptor();
combineInterceptor.setBeforeAdviceInterceptor(new MethodBeforeAdviceInterceptor(new MyBeforeAdvice()));
combineInterceptor.setAfterAdviceInterceptor(new MethodAfterAdviceInterceptor(new MyAfterAdvice()));
combineInterceptor.setAfterReturningAdviceInterceptor(new MethodAfterReturningAdviceInterceptor(new MyAfterReturningAdvice()));
combineInterceptor.setThrowsAdviceInterceptor(new MethodThrowsAdviceInterceptor(new MyThrowsAdvice()));

// 配置AOP信息
AdvisedSupport advisedSupport = new AdvisedSupport();
advisedSupport.setTargetSource(new TargetSource(worldService));
advisedSupport.setMethodInterceptor(combineInterceptor);
advisedSupport.setMethodMatcher(methodMatcher);

// 创建代理对象
WorldService proxy = (WorldService) new ProxyFactory(advisedSupport).getProxy();
```

**代理类型选择**:
```java
// JDK动态代理（默认）
advisedSupport.setProxyTargetClass(false);
WorldService proxy = (WorldService) new ProxyFactory(advisedSupport).getProxy();

// CGLIB代理
advisedSupport.setProxyTargetClass(true);
WorldService proxy = (WorldService) new ProxyFactory(advisedSupport).getProxy();
```

**切点表达式示例**:
```java
// 匹配指定类的所有方法
"execution(* com.example.service.UserService.*(..))"

// 匹配指定包下所有类的所有方法
"execution(* com.example.service.*.*(..))"

// 匹配指定方法
"execution(* com.example.service.UserService.save(..))"

// 匹配public方法
"execution(public * com.example.service.*.*(..))"
```

**输出结果示例**:
```
Do something before the earth explodes
The Earth is going to explode
Do something after the earth explodes
```

**AOP执行流程**:
1. 创建代理对象（JDK或CGLIB）
2. 拦截方法调用
3. 检查方法是否匹配切点条件
4. 如果匹配，执行MethodInterceptor
5. 如果不匹配，直接调用目标方法
6. 返回方法执行结果

**通知执行顺序**:
- 环绕通知优先级最高，如果配置了环绕通知，其他通知不会执行
- 否则按以下顺序：前置通知 → 目标方法 → 后置通知/返回后通知/异常通知

## 十、Bean作用域最佳实践 ⭐ 新增

### 10.1 作用域选择指南

**选择单例作用域的情况**:
- 无状态服务：工具类、配置类、服务类
- 性能敏感：避免重复创建对象的开销
- 资源共享：需要多个Bean共享同一个实例
- 生命周期管理：需要容器管理Bean的创建和销毁

**选择原型作用域的情况**:
- 有状态对象：包含实例变量的Bean
- 线程安全：避免多线程共享状态
- 临时对象：不需要长期保存的Bean
- 用户请求：每个请求需要独立的Bean实例

### 10.2 实际应用场景

**单例Bean示例**:
```java
// 配置服务 - 单例
public class ConfigService {
    private Map<String, String> config = new HashMap<>();
    
    public String getConfig(String key) {
        return config.get(key);
    }
}

// XML配置
<bean id="configService" class="com.example.ConfigService" scope="singleton"/>
```

**原型Bean示例**:
```java
// 用户会话 - 原型
public class UserSession {
    private String userId;
    private Date loginTime;
    private List<String> permissions;
    
    public UserSession(String userId) {
        this.userId = userId;
        this.loginTime = new Date();
        this.permissions = new ArrayList<>();
    }
}

// XML配置
<bean id="userSession" class="com.example.UserSession" scope="prototype"/>
```

### 10.3 性能考虑

**内存使用**:
- 单例Bean：内存占用低，但可能存在线程安全问题
- 原型Bean：内存占用高，但天然线程安全

**创建开销**:
- 单例Bean：只创建一次，后续从缓存获取
- 原型Bean：每次获取都创建，包含完整的初始化流程

**适用场景对比**:

| 场景 | 推荐作用域 | 原因 |
|------|------------|------|
| 数据库连接池 | Singleton | 资源共享，避免重复创建 |
| 用户会话 | Prototype | 每个用户独立状态 |
| 工具类 | Singleton | 无状态，性能优先 |
| 请求处理器 | Prototype | 有状态，线程安全 |
| 配置类 | Singleton | 全局共享配置 |
| 临时计算器 | Prototype | 避免状态污染 |

### 10.4 常见陷阱和注意事项

**单例Bean的线程安全问题**:
```java
// ❌ 错误：单例Bean包含可变状态
public class CounterService {
    private int count = 0; // 线程不安全
    
    public void increment() {
        count++; // 多线程访问会有问题
    }
}

// ✅ 正确：使用同步或原子类
public class CounterService {
    private AtomicInteger count = new AtomicInteger(0);
    
    public void increment() {
        count.incrementAndGet(); // 线程安全
    }
}
```

**原型Bean的生命周期管理**:
```java
// ❌ 错误：期望原型Bean执行销毁方法
<bean id="prototypeBean" class="com.example.PrototypeBean" 
      scope="prototype" destroy-method="cleanup"/>

// ✅ 正确：手动管理原型Bean的生命周期
public class PrototypeBean implements DisposableBean {
    @Override
    public void destroy() throws Exception {
        // 用户需要手动调用此方法
        cleanup();
    }
}
```

**作用域混用注意事项**:
- 单例Bean依赖原型Bean：每次获取都是新的原型Bean实例
- 原型Bean依赖单例Bean：所有原型Bean共享同一个单例Bean实例
- 避免循环依赖：原型Bean不支持循环依赖解决

### 10.5 FactoryBean使用示例 ⭐ 新增

**基本FactoryBean实现**:
```java
// 1. 实现FactoryBean接口
public class CarFactoryBean implements FactoryBean<Car> {
    private String brand;
    private String model;
    
    @Override
    public Car getObject() throws BeanException {
        Car car = new Car();
        car.setBrand(brand);
        car.setModel(model);
        return car;
    }
    
    @Override
    public boolean isSingleton() {
        return true; // 单例模式
    }
    
    // getter/setter方法...
}

// 2. XML配置
<bean id="car" class="com.example.CarFactoryBean">
    <property name="brand" value="porsche"/>
    <property name="model" value="911"/>
</bean>

// 3. 使用
ApplicationContext context = new ClassPathXmlApplicationContext("spring.xml");
Car car = context.getBean("car", Car.class);
```

**复杂对象创建示例**:
```java
// 数据库连接池FactoryBean
public class DataSourceFactoryBean implements FactoryBean<DataSource> {
    private String url;
    private String username;
    private String password;
    private int maxConnections = 10;
    
    @Override
    public DataSource getObject() throws BeanException {
        // 创建复杂的数据库连接池
        HikariConfig config = new HikariConfig();
        config.setJdbcUrl(url);
        config.setUsername(username);
        config.setPassword(password);
        config.setMaximumPoolSize(maxConnections);
        return new HikariDataSource(config);
    }
    
    @Override
    public boolean isSingleton() {
        return true; // 连接池应该是单例
    }
}

// XML配置
<bean id="dataSource" class="com.example.DataSourceFactoryBean">
    <property name="url" value="jdbc:mysql://localhost:3306/test"/>
    <property name="username" value="root"/>
    <property name="password" value="password"/>
    <property name="maxConnections" value="20"/>
</bean>
```

**原型FactoryBean示例**:
```java
// 用户会话FactoryBean
public class UserSessionFactoryBean implements FactoryBean<UserSession> {
    private String sessionId;
    
    @Override
    public UserSession getObject() throws BeanException {
        UserSession session = new UserSession();
        session.setSessionId(sessionId);
        session.setCreateTime(new Date());
        return session;
    }
    
    @Override
    public boolean isSingleton() {
        return false; // 每个用户会话都应该是独立的
    }
}
```

**FactoryBean与普通Bean的区别**:

| 特性 | 普通Bean | FactoryBean |
|------|----------|-------------|
| 对象类型 | 直接创建指定类型的对象 | 创建FactoryBean指定的对象类型 |
| 获取方式 | 直接通过Bean名称获取 | 通过Bean名称获取创建的对象 |
| 获取FactoryBean | 不适用 | 在Bean名称前加"&"前缀 |
| 缓存机制 | 根据作用域决定 | 根据isSingleton()决定 |
| 使用场景 | 简单对象创建 | 复杂对象创建、特殊初始化逻辑 |

**最佳实践**:
1. **单例FactoryBean**: 用于创建可共享的复杂对象（如连接池、配置对象）
2. **原型FactoryBean**: 用于创建有状态的对象（如用户会话、请求处理器）
3. **异常处理**: getObject()方法应该处理所有可能的异常
4. **线程安全**: 确保getObject()方法是线程安全的
5. **资源管理**: 在FactoryBean中正确管理资源生命周期

**XML配置文件示例 (spring.xml)**:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- 基本Bean定义 -->
    <bean id="userService" class="com.example.UserService">
        <property name="name" value="derek"/>
        <property name="age" value="25"/>
    </bean>

    <!-- Bean依赖注入 -->
    <bean id="orderService" class="com.example.OrderService">
        <property name="userService" ref="userService"/>
        <property name="orderId" value="12345"/>
    </bean>

    <!-- BeanFactoryPostProcessor -->
    <bean id="customBeanFactoryPostProcessor" class="com.example.CustomBeanFactoryPostProcessor"/>

    <!-- BeanPostProcessor -->
    <bean id="customBeanPostProcessor" class="com.example.CustomBeanPostProcessor"/>

</beans>
```

**BeanFactoryPostProcessor实现**:
```java
public class CustomBeanFactoryPostProcessor implements BeanFactoryPostProcess {
    @Override
    public void postProcessBeanFactory(ConfigurableListableBeanFactory beanFactory) {
        BeanDefinition beanDefinition = beanFactory.getBeanDefinition("person");
        PropertyValues propertyValues = beanDefinition.getPropertyValues();
        propertyValues.addPropertyValue(new PropertyValue("name", "ivy"));
    }
}
```

**BeanPostProcessor实现**:
```java
public class CustomerBeanPostProcessor implements BeanPostProcessor {
    @Override
    public Object postProcessBeforeInitialization(Object bean, String beanName) {
        if ("car".equals(beanName)) {
            ((Car) bean).setBrand("lamborghini");
        }
        return bean;
    }
    
    @Override
    public Object postProcessAfterInitialization(Object bean, String beanName) {
        return bean;
    }
}
```

---

## 十、学习要点

### 10.1 核心概念理解
1. **IoC 容器的本质**: 对象管理容器
2. **BeanDefinition**: 配置元数据与实例分离
3. **单例缓存**: 性能优化 + 单例模式

### 10.2 设计思想
1. **接口隔离**: 不同职责定义不同接口
2. **模板方法**: 定义算法骨架，细节由子类实现
3. **开闭原则**: 通过继承扩展功能，不修改原有代码

### 10.3 实现细节
1. **反射创建对象**: `Class.newInstance()` 的使用
2. **HashMap 缓存**: 单例对象的存储
3. **异常处理**: 统一的 BeanException

---

## 十一、常见问题

### Q1: 为什么 AbstractBeanFactory.getBean() 上加 @Override 会报错？
**A**: 因为接口方法签名不匹配。BeanFactory 接口定义的方法必须和实现类的方法签名完全一致（包括参数）。

### Q2: 为什么使用抽象类而不是接口？
**A**: 抽象类可以提供默认实现（如 getBean 的模板流程），而接口在 Java 8 之前不能有实现。

### Q3: 为什么需要 BeanDefinition？
**A**: 分离配置和实例。配置阶段注册 BeanDefinition，运行时根据 BeanDefinition 创建实例，支持懒加载。

### Q4: 单例 Bean 是否线程安全？
**A**: 容器保证单例，但 Bean 内部状态需要自行保证线程安全。

---

## 十二、版本更新说明 ⭐

### v1.13 - AOP支持 (2025/10/24)

**新增功能**:
1. ✅ 实现AOP核心组件（AdvisedSupport、TargetSource、MethodMatcher等）
2. ✅ 实现JDK动态代理支持（JdkDynamicAopProxy）
3. ✅ 实现CGLIB代理支持（CglibAopProxy）
4. ✅ 实现AspectJ表达式切点匹配（AspectJExpressionPointcut）
5. ✅ 实现反射方法调用（ReflectiveMethodInvocation）
6. ✅ 实现代理工厂（ProxyFactory）自动选择代理类型
7. ✅ 实现完整的通知类型支持（Before、After、AfterReturning、Throws、Around）
8. ✅ 实现通知拦截器适配器（MethodBeforeAdviceInterceptor等）
9. ✅ 实现组合通知拦截器（CombineAdviceInterceptor）
10. ✅ 实现AspectJ表达式切点通知器（AspectJExpressionPointcutAdvisor）
11. ✅ 添加完整的AOP测试用例和示例
12. ✅ 支持方法拦截和横切逻辑处理
13. ✅ 提供双重代理实现（JDK和CGLIB）
14. ✅ 完善所有AOP相关类的注释和文档

**技术亮点**:
- 双重代理支持：同时支持JDK动态代理和CGLIB代理
- 切点表达式：支持AspectJ表达式语法进行精确匹配
- 方法拦截：通过MethodInterceptor实现方法拦截
- 类型安全：通过泛型确保代理对象的类型安全
- 性能优化：CGLIB使用MethodProxy进行方法调用，性能更好
- 通知组合：支持多种通知类型的组合使用
- 接口继承匹配：实现类可以匹配接口的切点表达式
- 代理工厂：自动选择合适的代理实现
- 完整注释：所有AOP相关类都有详细的中文注释

**通知类型**:
- 前置通知（BeforeAdvice）：在目标方法执行前执行
- 后置通知（AfterAdvice）：在目标方法执行后执行
- 返回后通知（AfterReturningAdvice）：在目标方法正常返回后执行
- 异常通知（ThrowsAdvice）：在目标方法抛出异常时执行
- 环绕通知（AroundAdvice）：在目标方法执行前后都执行

**使用场景**:
- 日志记录：在方法执行前后记录日志
- 性能监控：统计方法执行时间
- 事务管理：在方法执行前后处理事务
- 安全检查：在方法执行前进行权限验证
- 缓存处理：在方法执行前后处理缓存

**AOP执行流程**:
1. 创建代理对象（JDK或CGLIB）
2. 拦截方法调用
3. 检查方法是否匹配切点条件
4. 如果匹配，执行MethodInterceptor
5. 如果不匹配，直接调用目标方法
6. 返回方法执行结果

**通知执行顺序**:
- 环绕通知优先级最高，如果配置了环绕通知，其他通知不会执行
- 否则按以下顺序：前置通知 → 目标方法 → 后置通知/返回后通知/异常通知

### v1.12 - 事件系统支持 (2025/10/21)

**新增功能**:
1. ✅ 实现完整的事件发布和监听机制
2. ✅ 添加ApplicationEvent抽象基类（应用事件基类）
3. ✅ 添加ApplicationListener接口（应用事件监听器接口）
4. ✅ 添加ApplicationEventPublisher接口（应用事件发布者接口）
5. ✅ 添加ApplicationEventMulticaster接口（应用事件广播器接口）
6. ✅ 实现SimpleApplicationEventMulticaster（简单事件广播器）
7. ✅ 实现容器生命周期事件（ContextRefreshedEvent、ContextClosedEvent）
8. ✅ 在AbstractApplicationContext中集成事件发布功能
9. ✅ 支持自定义事件和事件监听器
10. ✅ 添加完整的事件系统测试用例和示例

**技术亮点**:
- 观察者模式：实现事件发布者和监听者的解耦
- 泛型支持：通过泛型确保类型安全的事件监听
- 反射匹配：通过反射检查监听器是否支持特定事件类型
- 同步广播：事件广播过程是同步的，监听器按顺序执行
- 自动注册：监听器通过XML配置自动注册到事件广播器
- 异常隔离：单个监听器异常不影响其他监听器的执行

**使用场景**:
- 容器生命周期监听（启动、关闭等）
- 业务事件发布（用户注册、订单创建等）
- 系统状态变化通知
- 组件间松耦合通信
- 实现事件驱动的架构设计

**事件类型**:
- ContextRefreshedEvent：容器刷新完成事件
- ContextClosedEvent：容器关闭事件
- 自定义事件：继承自ApplicationEvent的业务事件

### v1.11 - FactoryBean支持 (2025/10/19)

**新增功能**:
1. ✅ 实现FactoryBean接口（用于创建复杂对象的工厂Bean）
2. ✅ 在AbstractBeanFactory中添加getObjectForBeanInstance()方法处理FactoryBean
3. ✅ 实现FactoryBean对象缓存池，支持单例FactoryBean的对象缓存
4. ✅ 支持FactoryBean的属性注入和生命周期管理
5. ✅ 区分FactoryBean本身和FactoryBean创建的对象
6. ✅ 添加完整的FactoryBean测试用例和示例

**技术亮点**:
- 延迟创建：只有在实际获取Bean时才调用getObject()方法
- 作用域支持：通过isSingleton()方法控制返回对象的作用域
- 对象分离：FactoryBean本身和其创建的对象是分离的
- 缓存机制：单例FactoryBean的getObject()结果会被缓存
- 类型安全：使用泛型确保返回对象的类型安全

**使用场景**:
- 创建复杂对象（如代理对象、连接池等）
- 需要特殊初始化逻辑的对象
- 需要根据配置动态创建的对象
- 需要包装或增强现有对象

### v1.10 - Bean作用域支持 (2025/10/19)

**新增功能**:
1. ✅ 实现Bean作用域支持（Singleton/Prototype）
2. ✅ 在BeanDefinition中添加作用域相关字段（scope、singleton、prototype）
3. ✅ 在AbstractAutowireCapableBeanFactory中根据作用域决定是否注册到缓存
4. ✅ 在XmlBeanDefinitionReader中解析scope属性
5. ✅ 支持默认作用域为singleton
6. ✅ 添加完整的prototype作用域测试用例

**技术亮点**:
- 作用域管理：支持单例和原型两种Bean作用域
- 生命周期差异：单例Bean由容器管理生命周期，原型Bean每次创建新实例
- 缓存策略：单例Bean注册到缓存，原型Bean不注册
- 销毁方法：只有单例Bean支持销毁方法
- XML配置：支持通过scope属性配置Bean作用域

**使用场景**:
- 单例Bean：无状态服务、工具类、配置类
- 原型Bean：有状态对象、线程安全Bean、临时Bean
- 性能优化：根据使用场景选择合适的作用域

### v1.9 - Aware接口功能 (2025/10/18)

**新增功能**:
1. ✅ 实现Aware标记接口（容器感知接口）
2. ✅ 实现BeanFactoryAware接口（BeanFactory感知接口）
3. ✅ 实现ApplicationContextAware接口（ApplicationContext感知接口）
4. ✅ 实现ApplicationContextAwareProcessor类（ApplicationContextAware处理器）
5. ✅ 在AbstractAutowireCapableBeanFactory中集成BeanFactoryAware处理
6. ✅ 在AbstractApplicationContext中集成ApplicationContextAwareProcessor
7. ✅ 完善Bean生命周期，支持容器感知功能

**技术亮点**:
- 容器感知机制：Bean可以主动感知容器组件
- 反向控制：Bean主动获取容器引用，而不是被动等待注入
- 优先级处理：BeanFactoryAware优先于ApplicationContextAware执行
- 专门处理器：ApplicationContextAwareProcessor专门处理ApplicationContextAware
- 生命周期集成：在Bean初始化过程中自动处理Aware接口

**使用场景**:
- Bean需要动态获取其他Bean实例
- Bean需要访问容器功能（如事件发布、资源获取等）
- 实现程序化的Bean查找和容器操作
- 支持容器感知的Bean设计模式

### v1.8 - Bean初始化和销毁方法功能 (2025/10/18)

**新增功能**:
1. ✅ 实现InitializingBean接口（Bean初始化回调接口）
2. ✅ 实现DisposableBean接口（Bean销毁回调接口）
3. ✅ 实现DisposableBeanAdapter类（Bean销毁适配器）
4. ✅ 完善BeanDefinition支持初始化方法和销毁方法配置
5. ✅ 在AbstractAutowireCapableBeanFactory中实现invokeInitMethods方法
6. ✅ 在DefaultSingletonBeanRegistry中实现destroySingletons方法
7. ✅ 支持XML配置中的init-method和destroy-method属性
8. ✅ 添加完整的Bean生命周期管理

**技术亮点**:
- Bean生命周期管理：完整的Bean初始化到销毁的生命周期管理
- 多种初始化方式：支持InitializingBean接口和自定义init-method方法
- 多种销毁方式：支持DisposableBean接口和自定义destroy-method方法
- 适配器模式：DisposableBeanAdapter统一处理不同销毁方式
- 反射调用：支持通过方法名调用初始化和销毁方法
- 避免重复：防止同时实现接口和配置方法时重复调用

**使用场景**:
- 资源初始化和清理（数据库连接、文件句柄等）
- 启动和停止后台服务
- 配置验证和清理工作
- 完整的Bean生命周期管理

### v1.7 - ApplicationContext应用上下文功能 (2025/10/18)

**新增功能**:
1. ✅ 实现ApplicationContext接口（应用上下文核心接口）
2. ✅ 实现ConfigurableApplicationContext接口（可配置应用上下文）
3. ✅ 实现AbstractApplicationContext抽象类（应用上下文抽象基类）
4. ✅ 实现AbstractRefreshableApplicationContext抽象类（可刷新应用上下文）
5. ✅ 实现AbstractXmlApplicationContext抽象类（XML应用上下文抽象类）
6. ✅ 实现ClassPathXmlApplicationContext类（ClassPath XML应用上下文）
7. ✅ 完善ApplicationContext的refresh()方法流程
8. ✅ 集成BeanFactoryPostProcessor和BeanPostProcessor支持

**技术亮点**:
- ApplicationContext：提供完整的应用上下文功能
- 模板方法模式：在AbstractApplicationContext中的应用
- 分层设计：从接口到抽象类到具体实现的完整层次
- 自动刷新：ClassPathXmlApplicationContext构造时自动刷新
- 资源集成：完美集成ResourceLoader进行资源加载

**使用场景**:
- 基于XML配置的Spring应用
- 需要从classpath加载配置的场景
- 简单的应用上下文配置
- 支持BeanFactoryPostProcessor和BeanPostProcessor的扩展

### v1.6 - BeanFactoryPostProcessor和BeanPostProcessor功能 (2025/10/18)

**新增功能**:
1. ✅ 实现BeanFactoryPostProcessor接口（BeanFactory后置处理器）
2. ✅ 实现BeanPostProcessor接口（Bean后置处理器）
3. ✅ 添加ConfigurableBeanFactory接口（可配置Bean工厂）
4. ✅ 添加AutowireCapableBeanFactory接口（自动装配Bean工厂）
5. ✅ 完善ConfigurableListableBeanFactory接口
6. ✅ 在AbstractBeanFactory中实现BeanPostProcessor管理
7. ✅ 在AbstractAutowireCapableBeanFactory中实现BeanPostProcessor执行
8. ✅ 添加完整的测试用例和示例代码

**技术亮点**:
- BeanFactoryPostProcessor：在Bean实例化前修改BeanDefinition
- BeanPostProcessor：在Bean实例化后、初始化前后对Bean进行处理
- 为AOP功能提供基础支持
- 完善的扩展点设计，支持自定义Bean处理逻辑

**使用场景**:
- 修改Bean的属性值
- 为Bean添加代理（AOP的基础）
- 实现Bean的增强功能
- 添加Bean的生命周期回调

### v1.5 - XML配置解析功能 (2025/10/17)

**新增功能**:
1. ✅ 实现XML配置解析模块
2. ✅ 添加 BeanDefinitionReader 接口（Bean定义读取规范）
3. ✅ 添加 AbstractBeanDefinitionReader 抽象基类
4. ✅ 实现 XmlBeanDefinitionReader（XML配置读取器）
5. ✅ 支持标准Spring XML配置格式
6. ✅ 支持bean标签和property标签解析
7. ✅ 支持value和ref两种属性注入方式
8. ✅ 支持多种Bean命名策略（id、name、类名首字母小写）
9. ✅ 添加完整的XML配置测试用例

**技术亮点**:
- 使用Hutool工具库简化XML解析
- 支持Bean之间的依赖注入（ref属性）
- 完善的异常处理和验证机制
- 与现有Resource模块完美集成

### v1.4 - Resource资源管理功能 (2025/10/16)

**新增功能**:
1. ✅ 实现Resource资源管理模块
2. ✅ 添加 Resource 接口（资源访问规范）
3. ✅ 添加 ResourceLoader 接口（资源加载规范）
4. ✅ 实现 DefaultResourceLoader（默认资源加载器）
5. ✅ 实现 ClassPathResource（classpath资源）
6. ✅ 实现 FileSystemResource（文件系统资源）
7. ✅ 实现 UrlResource（网络URL资源）
8. ✅ 添加完整的Resource测试用例

**技术亮点**:
- 策略模式在资源加载中的应用
- 统一的资源访问接口设计
- 支持多种资源类型（classpath、文件系统、URL）
- 完善的异常处理和容错机制

### v1.3 - Bean依赖注入功能 (2025/10/15)

**新增功能**:
1. ✅ 实现Bean之间的依赖注入功能
2. ✅ 添加 BeanReference 类（Bean依赖引用）
3. ✅ 支持BeanReference的自动解析和注入
4. ✅ 完善属性注入机制，支持Bean依赖
5. ✅ 添加完整的依赖注入测试用例

**技术亮点**:
- Bean依赖注入的完整实现
- BeanReference延迟解析机制
- 支持循环依赖的解决思路
- 完整的测试用例覆盖

### v1.2 - 属性注入功能 (2025/10/15)

**新增功能**:
1. ✅ 实现基本属性注入功能
2. ✅ 添加 PropertyValue 类（单个属性封装）
3. ✅ 添加 PropertyValues 类（属性集合管理）
4. ✅ 集成 Hutool 工具库简化反射操作
5. ✅ 支持基本类型和引用类型属性注入
6. ✅ 完善 BeanDefinition 支持属性值配置

**技术亮点**:
- 属性注入的完整实现
- Hutool 工具库的集成使用
- 类型自动转换和字段名匹配
- 详细的异常处理和调试信息

**依赖更新**:
- 新增 Hutool 5.5.0 依赖

### v1.1 - 实例化策略支持 (2025/10/12)

**新增功能**:
1. ✅ 实现策略模式，支持多种 Bean 实例化方式
2. ✅ 添加 SimpleInstantiationStrategy（JDK 反射）
3. ✅ 添加 CglibSubclassInstantiationStrategy（Cglib 代理）
4. ✅ 支持动态切换实例化策略
5. ✅ 为后续 AOP 实现打下基础

**技术亮点**:
- 策略模式的实践应用
- Cglib 动态代理技术
- 方法拦截器（MethodInterceptor）

**依赖更新**:
- 新增 Cglib 3.3.0 依赖

---

## 十三、总结

Winter Framework 是一个精简但完整的 IoC 容器实现，核心实现了：
- ✅ Bean 的创建和管理
- ✅ 单例模式和缓存机制
- ✅ 模板方法模式的优雅应用
- ✅ 策略模式实现实例化策略切换 ⭐
- ✅ JDK 反射和 Cglib 动态代理 ⭐
- ✅ **属性注入功能** ⭐ 新增
- ✅ **Hutool 工具库集成** ⭐ 新增
- ✅ **XML配置解析** ⭐ 新增
- ✅ **完整的AOP支持** ⭐ 新增

通过学习这个项目，可以深入理解：
1. Spring IoC 的核心原理
2. 多种设计模式在框架中的应用（模板方法、策略、工厂、单例）
3. 反射和元数据的使用
4. Cglib 动态代理技术
5. 框架的分层设计思想
6. **AOP的实现原理和设计思想** ⭐ 新增
7. **属性注入的实现原理** ⭐ 新增
8. **工具库的集成和使用** ⭐ 新增
9. **XML配置解析的实现原理** ⭐ 新增
10. **配置与代码分离的设计思想** ⭐ 新增
11. **面向切面编程的核心概念** ⭐ 新增
12. **动态代理技术的实际应用** ⭐ 新增
13. **切点表达式的匹配机制** ⭐ 新增
14. **通知类型的实现原理** ⭐ 新增

这是一个很好的学习 Spring 源码的入门项目！🎉

